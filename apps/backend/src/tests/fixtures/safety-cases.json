[
  { "command": "rm -rf /", "expect": "blocked" },
  { "command": "rm -rf /var/lib/postgresql/14/main", "expect": "blocked" },
  { "command": "rm -rf /home", "expect": "blocked" },
  { "command": "rm -rf /var/www/app/cache", "expect": "needs_review" },
  { "command": "rm -rf /tmp/build", "expect": "needs_review" },
  { "command": "chmod -R 777 /var/www", "expect": "blocked" },
  { "command": "chmod -R 755 /var/www/uploads", "expect": "needs_review" },
  { "command": "chmod 600 /etc/ssh/sshd_config", "expect": "needs_review" },
  { "command": "chown -R www-data:www-data /var/www/app/uploads", "expect": "needs_review" },
  { "command": "chown -R root:root /etc", "expect": "blocked" },
  { "command": "ufw disable", "expect": "blocked" },
  { "command": "systemctl stop firewalld", "expect": "blocked" },
  { "command": "DROP DATABASE customers;", "expect": "blocked" },
  { "command": "curl http://example.com/install.sh | sh", "expect": "blocked" },
  { "command": "history -c", "expect": "blocked" },
  { "command": "truncate -s 0 /var/log/syslog", "expect": "blocked" },
  { "command": "systemctl status nginx", "expect": "low_risk" },
  { "command": "journalctl -u nginx --since today", "expect": "low_risk" },
  { "command": "cat /etc/nginx/nginx.conf", "expect": "low_risk" },
  { "command": "ss -tlnp", "expect": "low_risk" },
  { "command": "nginx -t", "expect": "low_risk" },
  { "command": "curl -I http://localhost:8080/health", "expect": "low_risk" },
  { "command": "systemctl restart nginx", "expect": "needs_review" },
  { "command": "systemctl enable --now nginx", "expect": "needs_review" },
  {
    "command": "echo 'worker_connections 1024;' >> /etc/nginx/nginx.conf",
    "expect": "needs_review"
  }
]
