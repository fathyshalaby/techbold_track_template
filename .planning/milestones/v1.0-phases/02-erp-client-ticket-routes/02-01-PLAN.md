---
phase: 02-erp-client-ticket-routes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/phoenix/types.ts
autonomous: true
requirements:
  - ERP-01
  - ERP-04

must_haves:
  truths:
    - "TicketStatusSchema parses OPEN, PENDING, DONE and rejects unknown strings"
    - "TicketSchema validates all required fields and accepts optional sla_due_at/created_at as null"
    - "CustomerSystemSchema validates ticket_id, customer_id, and nested SystemInfo"
    - "ActivityCreateSchema validates required fields and accepts optional documentation fields"
    - "All six schemas are exported and produce inferred TypeScript types"
  artifacts:
    - path: "backend/src/phoenix/types.ts"
      provides: "Zod schemas and inferred types for all Phoenix OpenAPI entities"
      exports:
        - TicketStatusSchema
        - TicketSchema
        - SystemInfoSchema
        - CustomerSystemSchema
        - EmployeeSchema
        - CustomerSchema
        - ActivityCreateSchema
        - ActivitySchema
        - PhoenixErrorSchema
        - PhoenixValidationErrorSchema
        - Ticket
        - CustomerSystem
        - Employee
        - Customer
        - Activity
        - ActivityCreate
        - TicketStatus
  key_links:
    - from: "backend/src/phoenix/client.ts"
      to: "backend/src/phoenix/types.ts"
      via: "import of Zod schemas for response parsing"
      pattern: "from.*phoenix/types"
---

<objective>
Define all Zod schemas for the Phoenix OpenAPI contract. These schemas are the single source of
truth for runtime validation and TypeScript types across the Phoenix client, mock, and routes.

Purpose: Establish contracts before implementing the client — executor sees exact shapes needed.
Output: backend/src/phoenix/types.ts with all schemas and inferred types exported.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/phoenix-openapi.yaml
@backend/src/phoenix/types.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Zod schemas for all Phoenix OpenAPI entities</name>
  <files>backend/src/phoenix/types.ts</files>

  <read_first>
    - backend/src/phoenix/types.ts — current stub (export {})
    - docs/phoenix-openapi.yaml — locked schema source; read every schema under components/schemas
    - backend/src/env.ts — ESM import style with .js suffix; no import-time side effects pattern
  </read_first>

  <behavior>
    - TicketStatusSchema: z.enum(['OPEN', 'PENDING', 'DONE'])
    - TicketSchema: z.object with id:z.number(), title, description, priority, status:TicketStatusSchema, customer_id:z.number(), customer_name as required; tags:z.array(z.string()).optional(), sla_due_at:z.string().nullable().optional(), created_at:z.string().nullable().optional()
    - SystemInfoSchema: ip, port:z.number(), username, os required; notes optional
    - CustomerSystemSchema: ticket_id:z.number(), customer_id:z.number(), system:SystemInfoSchema — all required
    - EmployeeSchema: id, firstname, lastname, username, teamname — all required z.number()/z.string()
    - CustomerSchema: id, company_name, firstname, lastname, system:SystemInfoSchema — all required
    - ActivityCreateSchema: ticket_id:z.number(), start_datetime, end_datetime required; description, summary, root_cause, actions_taken, commands_summary, validation_result all optional z.string()
    - ActivitySchema: id, team_id, team_name, employee_id, ticket_id, start_datetime, end_datetime, description required; optional documentation fields; created_at nullable optional
    - PhoenixErrorSchema: z.object({ detail: z.string() })
    - PhoenixValidationErrorSchema: z.object({ detail: z.array(z.object({ loc: z.array(z.string()), msg: z.string(), type: z.string() })) })
    - Every schema has a corresponding inferred type export: `export type Ticket = z.infer<typeof TicketSchema>`
    - Test: TicketStatusSchema.parse('OPEN') succeeds; TicketStatusSchema.safeParse('INVALID').success === false
    - Test: TicketSchema.parse({id:1,title:'t',description:'d',priority:'high',status:'OPEN',customer_id:1,customer_name:'Acme'}) succeeds
    - Test: TicketSchema.safeParse({id:1,title:'t',description:'d',priority:'high',status:'OPEN',customer_id:1}) — missing customer_name — .success === false
    - Test: CustomerSystemSchema.parse({ticket_id:1,customer_id:1,system:{ip:'10.0.0.1',port:22,username:'azureuser',os:'Ubuntu 22.04'}}) succeeds
    - Test: ActivityCreateSchema.parse({ticket_id:1,start_datetime:'2026-01-01T00:00:00Z',end_datetime:'2026-01-01T01:00:00Z'}) succeeds (optional fields absent)
    - Test: TicketSchema with sla_due_at:null passes; with sla_due_at:'2026-01-01T00:00:00Z' passes
  </behavior>

  <action>
    Replace the stub in backend/src/phoenix/types.ts.

    Follow RED-GREEN-REFACTOR:
    RED: Create backend/src/tests/phoenix-types.test.ts (new file) with failing tests covering all
    behavior cases above. Import schemas from '../phoenix/types.js'. Run `cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npm test -- --reporter=verbose 2>&1 | tail -30` — confirm tests fail (import resolves to empty export {}).

    GREEN: Replace types.ts stub with full schema definitions. All schemas use z.object(). Use
    z.number() for all integer/int64 fields (JSON numbers, not BigInt). All required fields per
    OpenAPI required arrays; optional fields use .optional() or .nullable().optional(). Export inferred
    types with `export type Name = z.infer<typeof NameSchema>`. No import-time side effects — only
    imports from 'zod'. ESM: file has no default export; named exports only. Run tests — confirm green.

    REFACTOR: Group schemas logically (core entities → request/response shapes → error shapes).
    No runtime behaviour change. Re-run tests — still green.

    Do NOT add schemas beyond what docs/phoenix-openapi.yaml defines. Do NOT use z.bigint() —
    Phoenix returns JSON numbers which TypeScript/Zod handles as number.
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npm test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|phoenix-types)" | head -20</automated>
  </verify>

  <done>
    phoenix-types.test.ts passes all schema parse/reject cases. types.ts exports all ten schemas
    and their inferred types. No TypeScript errors on tsc --noEmit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix API → backend parser | Untrusted JSON from Phoenix crosses into typed domain here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | phoenix/types.ts schema parsing | mitigate | Use z.parse() (throws on invalid) not z.cast(); reject unknown fields with .strict() on schemas used as security boundaries — Ticket and CustomerSystem |
| T-02-02 | Information Disclosure | Zod error messages in test output | accept | Test environment only; error detail never reaches production logs in this plan |
</threat_model>

<verification>
- `npm test` in backend/ passes with phoenix-types.test.ts green
- `npx tsc --noEmit` in backend/ reports zero errors on types.ts
- `grep -c "export type" backend/src/phoenix/types.ts` returns at least 10
</verification>

<success_criteria>
All Phoenix OpenAPI entities are validated at the Zod boundary before any code can treat them as
typed. TicketSchema, CustomerSystemSchema, and ActivityCreateSchema parse correctly and reject
incomplete payloads. Every schema has a corresponding TypeScript type export.
</success_criteria>

<output>
Create .planning/phases/02-erp-client-ticket-routes/02-01-SUMMARY.md when done.
</output>

## Artifacts this phase plan produces

| Symbol | File | Kind |
|--------|------|------|
| TicketStatusSchema | backend/src/phoenix/types.ts | Zod schema |
| TicketSchema | backend/src/phoenix/types.ts | Zod schema |
| SystemInfoSchema | backend/src/phoenix/types.ts | Zod schema |
| CustomerSystemSchema | backend/src/phoenix/types.ts | Zod schema |
| EmployeeSchema | backend/src/phoenix/types.ts | Zod schema |
| CustomerSchema | backend/src/phoenix/types.ts | Zod schema |
| ActivityCreateSchema | backend/src/phoenix/types.ts | Zod schema |
| ActivitySchema | backend/src/phoenix/types.ts | Zod schema |
| PhoenixErrorSchema | backend/src/phoenix/types.ts | Zod schema |
| PhoenixValidationErrorSchema | backend/src/phoenix/types.ts | Zod schema |
| Ticket, CustomerSystem, Employee, Customer, Activity, ActivityCreate, TicketStatus | backend/src/phoenix/types.ts | Inferred TS types |
| phoenix-types.test.ts | backend/src/tests/phoenix-types.test.ts | Test file |
</output>
