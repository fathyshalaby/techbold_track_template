---
phase: 04-ssh-executor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/ssh/types.ts
autonomous: true
requirements:
  - DIAG-06
must_haves:
  truths:
    - "CommandResult has exactly 5 fields: exitCode, stdout, stderr, durationMs, timedOut"
    - "SSHConnectionError is a typed error class surfacing connect/auth failures"
    - "PreflightResult records sudo capability, LANG, and PATH so the orchestrator can act on it"
    - "SshExecutor interface is the contract both real and mock must satisfy"
  artifacts:
    - path: backend/src/ssh/types.ts
      provides: "All SSH layer type exports"
      exports:
        - CommandResult
        - PreflightResult
        - SshConnectionError
        - SshExecutor
        - SshTarget
  key_links:
    - from: backend/src/ssh/executor.ts
      to: backend/src/ssh/types.ts
      via: "import { CommandResult, PreflightResult, SshConnectionError }"
      pattern: "CommandResult"
    - from: backend/src/ssh/mock.ts
      to: backend/src/ssh/types.ts
      via: "implements SshExecutor"
      pattern: "SshExecutor"
---

<objective>
Define all type contracts for the SSH layer before any implementation code is written.

Purpose: Downstream plans (04-02 through 04-05) write against these interfaces without ambiguity. Interface-first ordering prevents the scavenger-hunt anti-pattern where executor and mock diverge in shape.

Output: `backend/src/ssh/types.ts` with five exports: `CommandResult`, `PreflightResult`, `SshConnectionError`, `SshTarget`, `SshExecutor` interface. Replaces the empty stub.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/ROADMAP.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/phases/04-ssh-executor/04-CONTEXT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/docs/ARCHITECTURE.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/ssh/types.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/phoenix/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define SSH layer type contracts in types.ts (per DIAG-06, ARCHITECTURE §3)</name>
  <files>backend/src/ssh/types.ts, backend/src/tests/ssh-types.test.ts</files>

  <read_first>
    - backend/src/ssh/types.ts (current stub — empty export {})
    - docs/ARCHITECTURE.md §3 lines 156–160 (CommandResult shape contract)
    - docs/ARCHITECTURE.md §2 ssh/ folder listing (SshExecutor interface placement)
    - backend/src/safety/redaction.ts (REDACTION_CAP_BYTES = 16384 — output cap constant lives here, not in types)
    - backend/src/phoenix/types.ts (reference for Zod-defined type pattern used in this codebase)
  </read_first>

  <behavior>
    - CommandResult interface has exactly: exitCode (number), stdout (string), stderr (string), durationMs (number), timedOut (boolean)
    - PreflightResult interface has: sudoAvailable (boolean), lang (string), path (string)
    - SshConnectionError is a class extending Error with name "SSHConnectionError" and a typed cause field
    - SshTarget interface has: host (string), port (number), username (string), privateKeyPath (string)
    - SshExecutor interface has: executeApprovedCommand(approvalId: string, command: string, target: SshTarget): Promise<CommandResult>; runPreflight(target: SshTarget): Promise<PreflightResult>
    - All five symbols are named exports (no default export)
    - Test: constructing SshConnectionError('msg') yields instance.name === "SSHConnectionError"
    - Test: CommandResult object literal satisfies the interface (TypeScript structural check via assignability test)
  </behavior>

  <action>
    Write backend/src/ssh/types.ts replacing the empty stub.

    Export these five symbols exactly (per DIAG-06, ARCHITECTURE.md §3):

    SshTarget — host: string, port: number, username: string, privateKeyPath: string.

    CommandResult — exitCode: number, stdout: string, stderr: string, durationMs: number, timedOut: boolean. These are the EXACT five fields from ARCHITECTURE.md §3 line 157. Do not add, rename, or omit any field.

    PreflightResult — sudoAvailable: boolean, lang: string, path: string. The sudoAvailable flag implements Safety Policy G7: when false, the orchestrator surfaces "sudo unavailable" to the agent instead of failing the run.

    SshConnectionError — class extending Error. Constructor takes message: string and optional cause: unknown. Sets this.name = 'SSHConnectionError'. This typed error is thrown by client.ts on connect/auth failure and caught by the orchestrator for clean run-error surfacing.

    SshExecutor — interface with two methods:
      executeApprovedCommand(approvalId: string, command: string, target: SshTarget): Promise<CommandResult>
      runPreflight(target: SshTarget): Promise<PreflightResult>

    The approvalId parameter in executeApprovedCommand links execution back to the command_approvals store row (Phase 6 wiring). It is part of the interface contract now even though Phase 4 executor does not read from the store yet.

    No Zod schemas in types.ts — these are TypeScript interfaces only. The boundary validation for SSH results lives in the store layer (Phase 6).

    Write backend/src/tests/ssh-types.test.ts with two tests:
    1. SSHConnectionError name: construct new SshConnectionError('connect failed') and assert instance.name === 'SSHConnectionError' and instance.message === 'connect failed'
    2. CommandResult structural: assign a complete object literal to a CommandResult variable and assert each field type via typeof checks — this proves the interface compiles with the exact 5-field shape
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx vitest run src/tests/ssh-types.test.ts 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - backend/src/ssh/types.ts exports: CommandResult, PreflightResult, SshConnectionError, SshTarget, SshExecutor
    - CommandResult has exactly 5 fields matching ARCHITECTURE.md §3: exitCode (number), stdout (string), stderr (string), durationMs (number), timedOut (boolean)
    - SshExecutor interface declares executeApprovedCommand and runPreflight with the exact signatures above
    - SshConnectionError.name === 'SSHConnectionError' (vitest test passes)
    - vitest run ssh-types exits 0
    - No default export; all five symbols are named exports
  </acceptance_criteria>

  <done>types.ts is non-empty, all five symbols are exported, ssh-types.test.ts passes, and tsc compiles without errors on the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| types.ts → executor.ts | Type contracts cross from definition to implementation; a shape mismatch here causes silent runtime mismatches |
| SshTarget.privateKeyPath → file system | The key path is a string in memory; it must never be logged or echoed — the executor (not types) is responsible for file I/O, but the interface carries the field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | SshTarget.privateKeyPath | mitigate | Type carries only the path string, never the key bytes. Executor reads key at connect time; key content never stored on the type instance. Logging of SshTarget objects is forbidden — enforced by redaction in callers. |
| T-04-02 | Tampering | CommandResult shape drift | mitigate | Interface-first plan: all downstream plans import from types.ts; shape changes here break compilation immediately rather than silently at runtime. |
| T-04-SC | Tampering | npm install (no new installs this plan) | accept | No package-manager installs in this plan — types.ts is pure TypeScript interfaces. |
</threat_model>

<verification>
- vitest run src/tests/ssh-types.test.ts → exits 0, both type tests pass
- grep -n "executeApprovedCommand\|CommandResult\|PreflightResult\|SshConnectionError\|SshTarget\|SshExecutor" backend/src/ssh/types.ts shows all 5 exports present
- tsc --noEmit in backend/ has no errors on types.ts
</verification>

<success_criteria>
All five SSH layer type contracts are defined and compilable. Every downstream plan can import from ssh/types.ts without ambiguity.
</success_criteria>

<output>
Create `.planning/phases/04-ssh-executor/04-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `CommandResult` | interface | `backend/src/ssh/types.ts` |
| `PreflightResult` | interface | `backend/src/ssh/types.ts` |
| `SshConnectionError` | class | `backend/src/ssh/types.ts` |
| `SshTarget` | interface | `backend/src/ssh/types.ts` |
| `SshExecutor` | interface | `backend/src/ssh/types.ts` |
