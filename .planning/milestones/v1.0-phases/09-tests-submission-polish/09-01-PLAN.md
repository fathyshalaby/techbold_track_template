# 09-01 Plan: Root Test Command And Clean Automated Checks

## Goal

Make repository-level verification match the roadmap: `pnpm test` should run the backend and frontend checks successfully.

## Tasks

- Add root `package.json` with `test` and `build` scripts.
- Run backend tests and fix any real failures or unhandled Vitest errors.
- Run frontend tests.
- Run frontend production build.

## Verification

- `pnpm test`
- `pnpm --dir frontend build`

