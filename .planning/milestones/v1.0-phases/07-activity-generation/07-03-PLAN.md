---
phase: 07-activity-generation
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - backend/src/app.ts
autonomous: true
requirements:
  - ACT-02

must_haves:
  truths:
    - "POST /api/runs/:id/activity/draft is reachable via the main app (not just the isolated testApp)"
    - "POST /api/runs/:id/activity/submit is reachable via the main app"
    - "activityRouter mounts under /api/runs alongside runsRouter, approvalsRouter, eventsRouter"
  artifacts:
    - path: "backend/src/app.ts"
      provides: "activityRouter mounted under /api/runs"
      contains: "activityRouter"
  key_links:
    - from: "backend/src/app.ts"
      to: "backend/src/routes/activity.ts"
      via: "import activityRouter"
      pattern: "activityRouter"
---

<objective>
Mount activityRouter into app.ts and run the full test suite to confirm no regressions.

Purpose: The two activity endpoints must be reachable through the same Hono app instance the frontend and integration tests use. This is a single-line mount — identical to how approvalsRouter and eventsRouter are already wired.

Output: `app.ts` imports and mounts `activityRouter` on `/api/runs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-activity-generation/07-01-SUMMARY.md
@.planning/phases/07-activity-generation/07-02-SUMMARY.md
</context>

## Artifacts this phase produces (plan 07-03)

| Symbol | File | Notes |
|--------|------|-------|
| `app.route('/api/runs', activityRouter)` | `backend/src/app.ts` | Mount alongside existing routers |

<tasks>

<task type="auto">
  <name>Task 1: Mount activityRouter in app.ts + verify full suite</name>
  <files>backend/src/app.ts</files>

  <read_first>
  - backend/src/app.ts — current imports and `app.route(...)` call list; add activityRouter after eventsRouter following the same pattern
  </read_first>

  <action>
  In `backend/src/app.ts`:
  1. Add `import { activityRouter } from './routes/activity.js';` alongside the existing router imports
  2. Add `app.route('/api/runs', activityRouter);` immediately after `app.route('/api/runs', eventsRouter);`

  No other changes. Do not alter CORS config, error handler, or other routes.

  Then run the full backend test suite to confirm all tests pass including the 11 new activity tests.
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx vitest run 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
  - `backend/src/app.ts` contains `import { activityRouter } from './routes/activity.js'`
  - `backend/src/app.ts` contains `app.route('/api/runs', activityRouter)`
  - Full vitest run: all pre-existing tests green, all 11 activity tests green, zero new failures
  - `grep -c 'activityRouter' backend/src/app.ts` returns at least 2 (import line + mount line)
  </acceptance_criteria>

  <done>app.ts mounts activityRouter; full test suite is green with no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.ts router mount | No new trust boundary introduced — same Hono app already exposes /api/runs/* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | Route collision — /api/runs/* path conflict with runsRouter | accept | Hono matches specific paths first; `/activity/draft` and `/activity/submit` do not collide with `/:runId` GET or `/:runId/next` POST patterns verified by test suite |
| T-07-SC | Tampering | npm installs | accept | No new packages — mount is import + one line |
</threat_model>

<verification>
```
cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx vitest run 2>&1 | tail -30
```

All tests pass. No regressions.
</verification>

<success_criteria>
- `app.ts` has activityRouter import and mount
- Full test suite green
- `POST /api/runs/:runId/activity/draft` and `POST /api/runs/:runId/activity/submit` routable through the app
</success_criteria>

<output>
Create `.planning/phases/07-activity-generation/07-03-SUMMARY.md` when done
</output>
