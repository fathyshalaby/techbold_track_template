---
phase: 07-activity-generation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/ai/types.ts
  - backend/src/ai/prompts.ts
  - backend/src/ai/agents/activity-log-generator.ts
  - backend/src/tests/activity-log-generator.test.ts
autonomous: true
requirements:
  - ACT-01

must_haves:
  truths:
    - "runActivityLogGenerator called with audit events + command results returns an object with all 5 camelCase fields populated"
    - "MOCK_ACTIVITY_DRAFT is an exported constant with all 5 fields non-empty"
    - "AgentUnavailableError is thrown when the model times out or rejects"
    - "ActivityDraftFieldsSchema is exported from ai/types.ts with all 5 required string fields"
    - "ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT is exported from ai/prompts.ts"
  artifacts:
    - path: "backend/src/ai/types.ts"
      provides: "ActivityDraftFieldsSchema + ActivityDraftFields type"
      contains: "ActivityDraftFieldsSchema"
    - path: "backend/src/ai/prompts.ts"
      provides: "ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT"
      contains: "ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT"
    - path: "backend/src/ai/agents/activity-log-generator.ts"
      provides: "runActivityLogGenerator, MOCK_ACTIVITY_DRAFT"
      exports: ["runActivityLogGenerator", "MOCK_ACTIVITY_DRAFT", "ActivityLogGeneratorInput"]
    - path: "backend/src/tests/activity-log-generator.test.ts"
      provides: "TDD test suite"
      min_lines: 50
  key_links:
    - from: "backend/src/ai/agents/activity-log-generator.ts"
      to: "backend/src/ai/types.ts"
      via: "import ActivityDraftFieldsSchema"
      pattern: "ActivityDraftFieldsSchema"
    - from: "backend/src/ai/agents/activity-log-generator.ts"
      to: "backend/src/ai/prompts.ts"
      via: "import ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT"
      pattern: "ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT"
---

<objective>
Implement the activity-log-generator agent: audit trail → 5 graded ERP fields.

Purpose: Satisfies ACT-01 — the agent produces all 5 graded fields (`summary`, `rootCause`, `actionsTaken`, `commandsSummary`, `validationResult`) from audit data only, with no invented facts and no secrets.

Output: `ActivityDraftFieldsSchema` in `ai/types.ts`, `ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT` in `ai/prompts.ts`, `runActivityLogGenerator` + `MOCK_ACTIVITY_DRAFT` in `ai/agents/activity-log-generator.ts`, and a green TDD test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-activity-generation/07-CONTEXT.md
</context>

## Artifacts this phase produces (plan 07-01)

| Symbol | File | Notes |
|--------|------|-------|
| `ActivityDraftFieldsSchema` | `backend/src/ai/types.ts` | Zod object, 5 camelCase string fields |
| `ActivityDraftFields` | `backend/src/ai/types.ts` | Inferred TS type |
| `ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT` | `backend/src/ai/prompts.ts` | System prompt string |
| `ActivityLogGeneratorInput` | `backend/src/ai/agents/activity-log-generator.ts` | Input type exported |
| `runActivityLogGenerator` | `backend/src/ai/agents/activity-log-generator.ts` | Main agent function |
| `MOCK_ACTIVITY_DRAFT` | `backend/src/ai/agents/activity-log-generator.ts` | Mock constant |
| `activity-log-generator.test.ts` | `backend/src/tests/` | TDD test file |

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for runActivityLogGenerator</name>
  <files>backend/src/tests/activity-log-generator.test.ts, backend/src/ai/types.ts, backend/src/ai/agents/activity-log-generator.ts</files>

  <read_first>
  - backend/src/ai/agents/validator.ts — canonical agent shape: optional model param, Promise.race timeout (30_000ms), AgentUnavailableError, MOCK_* constant, generateObject pattern
  - backend/src/ai/types.ts — existing schemas to understand file structure before adding ActivityDraftFieldsSchema
  - backend/src/ai/agents/activity-log-generator.ts — current stub (2-line export {})
  - backend/src/tests/approvals.test.ts — test file conventions: vi.mock env, makeJsonlAdapter, setDb/resetDb, vi.spyOn describe-scoped, vi.clearAllMocks afterEach
  - backend/src/store/schema.ts — CommandResultSchema and AuditEventSchema shapes: field names used in agent input construction
  </read_first>

  <behavior>
  - Test 1 (happy path via injected model): `runActivityLogGenerator(input, mockModel)` resolves with an object where all 5 fields (`summary`, `rootCause`, `actionsTaken`, `commandsSummary`, `validationResult`) are non-empty strings matching the mock model's scripted output
  - Test 2 (MOCK_ACTIVITY_DRAFT constant): `MOCK_ACTIVITY_DRAFT` is an object where each of the 5 fields is a non-empty string (validates the constant is well-formed for offline use)
  - Test 3 (agent unavailable): when the injected model rejects, `runActivityLogGenerator` throws `AgentUnavailableError`
  - Test 4 (timeout): when the injected model never resolves within 30s (use vi.useFakeTimers + advance 31s), `runActivityLogGenerator` throws `AgentUnavailableError`
  - Test 5 (commandsSummary grounding): the input's `commandResults` array items (command + exitCode) appear in the constructed prompt string passed to generateObject — spy on generateObject import to assert the prompt JSON contains the command string
  </behavior>

  <action>
  Write `backend/src/tests/activity-log-generator.test.ts` first. The test file must:
  - Import `runActivityLogGenerator`, `MOCK_ACTIVITY_DRAFT`, `ActivityLogGeneratorInput` from `'../ai/agents/activity-log-generator.js'`
  - Import `AgentUnavailableError` from `'../ai/agents/problem-analyzer.js'`
  - Mock `'../env.js'` the same way approvals.test.ts does (resolveClientMode → 'mock', getEnv returns stub values)
  - Use `makeJsonlAdapter` + `setDb`/`resetDb` pattern for store isolation even though these tests don't hit routes (keeps consistent test hygiene)
  - Inject a LanguageModelV1 mock via the optional `model` param (do not use vi.mock on the ai module — inject directly per Phase 05 decision)
  - For the injected model, use `createMockLanguageModel` helper or a plain object with `doGenerate` that returns the scripted 5-field object wrapped in the Vercel AI SDK `generateObject` response shape

  Then stub `backend/src/ai/agents/activity-log-generator.ts` just enough for the import to resolve without error (export the type `ActivityLogGeneratorInput`, export a placeholder `runActivityLogGenerator` that immediately throws `new Error('not implemented')`, export `MOCK_ACTIVITY_DRAFT` as an empty object `{}` cast to the right type). This makes tests RED.

  Add `ActivityDraftFieldsSchema` to `backend/src/ai/types.ts`:
  ```
  z.object({
    summary: z.string(),
    rootCause: z.string(),
    actionsTaken: z.string(),
    commandsSummary: z.string(),
    validationResult: z.string(),
  })
  ```
  Export the inferred type `ActivityDraftFields`. Do not use fenced code in action — the above is the schema shape in prose form: five required string fields, camelCase names matching the CONTEXT.md decision.

  Run `cd backend && npx vitest run src/tests/activity-log-generator.test.ts` — all tests MUST fail (RED gate).
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx vitest run src/tests/activity-log-generator.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists, imports resolve, all tests fail with "not implemented" or schema mismatch — RED confirmed</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement runActivityLogGenerator</name>
  <files>backend/src/ai/prompts.ts, backend/src/ai/agents/activity-log-generator.ts</files>

  <read_first>
  - backend/src/ai/agents/validator.ts — exact implementation shape to mirror: imports, Promise.race pattern, generateObject call, error catch → AgentUnavailableError, maxTokens
  - backend/src/ai/prompts.ts — existing prompt style (SAFETY_PREAMBLE usage, role/field description format) to match before appending ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT
  - backend/src/ai/types.ts — confirm ActivityDraftFieldsSchema was added in Task 1
  - backend/src/store/schema.ts — AuditEvent.payload_json is a string; CommandResult has command, exit_code, stdout_redacted, stderr_redacted; Observation has content
  </read_first>

  <behavior>
  GREEN: all 5 tests from Task 1 pass.
  </behavior>

  <action>
  Add `ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT` to `backend/src/ai/prompts.ts`. The prompt must:
  - Open with `${SAFETY_PREAMBLE}` (reuse the existing constant, same as all other agent prompts)
  - Role line: `Role: activity_log_generator — ERP activity report drafter.`
  - Instruction: every field must trace to supplied audit data; if evidence is missing say so plainly rather than fabricate; invent nothing; no secrets in output
  - Field descriptions for all 5 (summary, rootCause, actionsTaken, commandsSummary, validationResult) matching the CONTEXT.md specifics: commandsSummary enumerates executed commands (command + exit code) from the supplied commandResults list for traceability
  - Constraint: commandsSummary must enumerate only commands actually listed in the input commandResults array — do not reference commands not present in the data

  Replace the stub in `backend/src/ai/agents/activity-log-generator.ts` with the full implementation mirroring validator.ts exactly:
  - Imports: `generateObject` from 'ai', `LanguageModelV1` from 'ai', `getModel` from '../model.js', `ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT` from '../prompts.js', `ActivityDraftFieldsSchema` and `ActivityDraftFields` from '../types.js', `AgentUnavailableError` from './problem-analyzer.js'
  - Export `AgentUnavailableError` (re-export, same as validator.ts)
  - Export type `ActivityLogGeneratorInput`: fields `auditEvents: Array<{type: string; actor: string; ts: string; payload_json: string}>`, `commandResults: Array<{command: string; exitCode: number; stdoutRedacted: string; stderrRedacted: string}>`, `observations: string[]`, `ticketDescription: string`
  - Export `MOCK_ACTIVITY_DRAFT` constant typed as `ActivityDraftFields` with all 5 fields set to realistic non-empty strings (one sentence each, no secrets)
  - Export `async function runActivityLogGenerator(input: ActivityLogGeneratorInput, model?: LanguageModelV1): Promise<ActivityDraftFields>`
  - Body: `const resolvedModel = model ?? getModel()`, then `Promise.race([generateObject({model: resolvedModel, schema: ActivityDraftFieldsSchema, system: ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT, prompt: JSON.stringify(input), maxTokens: 2048}), new Promise<never>((_, rej) => setTimeout(() => rej(new Error('timeout')), 30_000))])`, return `result.object`, catch all → throw `new AgentUnavailableError('agent unavailable: activity-log-generator')`
  - maxTokens is 2048 (higher than other agents — activity report fields need room)

  Run tests — all 5 MUST pass (GREEN gate).
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx vitest run src/tests/activity-log-generator.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 activity-log-generator tests pass. MOCK_ACTIVITY_DRAFT has 5 non-empty string fields. runActivityLogGenerator throws AgentUnavailableError on model failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit store → agent input | Audit events are pre-redacted at store-write time; this agent reads them back and assembles the LLM prompt |
| agent output → caller | LLM output is structured via Zod; caller (route, plan 07-02) must still pass through redactSecrets before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `runActivityLogGenerator` prompt assembly | mitigate | Input is pre-redacted by store writes; `redactSecrets` applied again at route layer before `saveActivityDraft` (defence-in-depth per CONTEXT.md decision) |
| T-07-02 | Tampering | LLM output — fabricated facts in draft fields | mitigate | System prompt instructs "invent nothing; trace every claim to supplied data"; Zod schema enforces shape but not truthfulness — graders evaluate output quality, not schema compliance |
| T-07-03 | Denial of Service | 30s timeout race in Promise.race | accept | Same pattern as all other agents; timeout throws AgentUnavailableError so the route returns 502 rather than hanging indefinitely |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all imports already in package.json |
</threat_model>

<verification>
Run the full backend test suite to confirm no regressions:

```
cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx vitest run 2>&1 | tail -30
```

All pre-existing tests must remain green. New activity-log-generator tests must be green.
</verification>

<success_criteria>
- `ActivityDraftFieldsSchema` exported from `backend/src/ai/types.ts` with 5 required string fields (camelCase)
- `ACTIVITY_LOG_GENERATOR_SYSTEM_PROMPT` exported from `backend/src/ai/prompts.ts`, opens with SAFETY_PREAMBLE, instructs no fabrication
- `runActivityLogGenerator` in `backend/src/ai/agents/activity-log-generator.ts`: optional model param, 30s Promise.race timeout, throws AgentUnavailableError on failure, returns `ActivityDraftFields`
- `MOCK_ACTIVITY_DRAFT` exported with all 5 non-empty string fields
- All 5 TDD tests pass (happy path, mock constant, agent-unavailable, timeout, commandsSummary grounding)
- Full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/07-activity-generation/07-01-SUMMARY.md` when done
</output>
