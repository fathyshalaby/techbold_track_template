---
phase: 01-dashboard-ownership-and-data-contract
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - package.json
  - docker-compose.yml
  - apps/dashboard/Dockerfile
  - .env.example
  - docs/ARCHITECTURE.md
  - docs/API.md
  - docs/README.md
  - docs/LIMITATIONS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DASH-04

must_haves:
  truths:
    - "Root scripts make the Next.js dashboard the primary UI path while retaining Vite as an explicit temporary fallback."
    - "Docker Compose serves the dashboard as the primary frontend service and documents Vite fallback criteria."
    - "Documentation states that Hono remains source of truth for Phoenix, SSH, LLM, safety, approvals, audit, SSE, activity, memory rules, and observability rules."
    - "Vite fallback retirement criteria are concrete and tied to parity, smoke verification, and no Phase 1 regressions."
    - "Phase 1 traceability marks DASH-01 through DASH-04 covered only after implementation and verification evidence exists."
    - "The executor checked the dirty worktree before editing and did not revert unrelated user changes, especially existing .env.example changes."
  artifacts:
    - path: "package.json"
      provides: "Primary dashboard scripts and explicit Vite fallback scripts."
    - path: "docker-compose.yml"
      provides: "Primary dashboard service wiring."
    - path: "apps/dashboard/Dockerfile"
      provides: "Container build for Next.js dashboard."
    - path: "docs/ARCHITECTURE.md"
      provides: "UI ownership decision and Vite fallback retirement criteria."
    - path: "docs/API.md"
      provides: "Dashboard endpoint and extended run detail contract documentation."
---

<objective>
Make dashboard ownership operationally explicit through scripts, Docker, documentation, smoke verification, and traceability updates.
</objective>

<context>
Read the phase source of truth before editing:

- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-CONTEXT.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-RESEARCH.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-PATTERNS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md
- AGENTS.md

Dirty worktree risk: run `git status --short` before editing. .env.example is already modified by someone else in the current worktree. Read it before editing and apply only additive, necessary dashboard environment changes. Do not revert unrelated lines.
</context>

<tasks>
  <task id="01" type="auto">
    <name>Update root scripts and environment for dashboard primary path</name>
    <read_first>
      <file>package.json</file>
      <file>apps/dashboard/package.json</file>
      <file>apps/frontend/package.json</file>
      <file>.env.example</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-CONTEXT.md</file>
    </read_first>
    <action>
      Update root package.json scripts so the Next.js dashboard is the primary UI path:

      - "dev:frontend": "bun run --filter techbold-track-dashboard dev"
      - "dev:dashboard": "bun run --filter techbold-track-dashboard dev"
      - "dev:vite": "bun run --filter techbold-track-frontend dev"
      - "build": includes the dashboard build as the primary frontend build.
      - "test": includes backend, dashboard, and Vite fallback tests.
      - "typecheck": includes backend, dashboard, contracts, and Vite fallback typechecks.

      Keep Vite available as techbold-track-frontend and dev:vite. Add NEXT_PUBLIC_API_BASE=http://localhost:8000 to .env.example if it is absent. Do not remove existing VITE_API_BASE while Vite is retained as fallback.
    </action>
    <acceptance_criteria>
      <criterion>`bun run dev:frontend` starts techbold-track-dashboard, not techbold-track-frontend.</criterion>
      <criterion>`bun run dev:vite` remains available for the temporary Vite fallback.</criterion>
      <criterion>Root build, test, and typecheck scripts include @techbold/contracts and techbold-track-dashboard.</criterion>
      <criterion>.env.example contains NEXT_PUBLIC_API_BASE and keeps VITE_API_BASE while Vite fallback exists.</criterion>
      <criterion>Existing unrelated .env.example changes are preserved.</criterion>
    </acceptance_criteria>
  </task>

  <task id="02" type="auto">
    <name>Move Docker Compose primary frontend service to dashboard</name>
    <read_first>
      <file>docker-compose.yml</file>
      <file>apps/frontend/Dockerfile</file>
      <file>apps/dashboard/Dockerfile</file>
      <file>apps/dashboard/package.json</file>
      <file>package.json</file>
    </read_first>
    <action>
      Create apps/dashboard/Dockerfile for the Next.js dashboard package using the current Bun workspace. It must install dependencies from the root workspace, build techbold-track-dashboard, expose port 3000, and run the dashboard start script.

      Update docker-compose.yml so the primary frontend service builds apps/dashboard/Dockerfile, sets NEXT_PUBLIC_API_BASE=http://localhost:8000, publishes "3000:3000", and depends_on backend. Retain the Vite Dockerfile but do not keep it as the primary frontend service. If a Vite fallback compose service is added, name it frontend-vite, publish it on 5173, and place it behind a fallback profile so it does not compete with the primary service by default.
    </action>
    <acceptance_criteria>
      <criterion>docker-compose.yml primary frontend service uses apps/dashboard/Dockerfile and port 3000.</criterion>
      <criterion>Vite, if present in compose, is named frontend-vite and behind a non-default profile.</criterion>
      <criterion>Docker Compose does not expose two primary frontend services by default.</criterion>
      <criterion>apps/dashboard/Dockerfile does not bake Phoenix tokens, SSH key paths, or other secrets into the image.</criterion>
      <criterion>`bun run --filter techbold-track-dashboard build` succeeds before Docker build verification.</criterion>
    </acceptance_criteria>
  </task>

  <task id="03" type="auto">
    <name>Document dashboard ownership, API contracts, and Vite retirement criteria</name>
    <read_first>
      <file>docs/ARCHITECTURE.md</file>
      <file>docs/API.md</file>
      <file>docs/README.md</file>
      <file>docs/LIMITATIONS.md</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-CONTEXT.md</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-RESEARCH.md</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md</file>
      <file>packages/contracts/src/dashboard.ts</file>
      <file>packages/contracts/src/runs.ts</file>
    </read_first>
    <action>
      Update docs/ARCHITECTURE.md with an explicit UI ownership section:

      - Next.js apps/dashboard is the primary operational UI path for v1.3 onward.
      - apps/frontend Vite is retained temporarily as compatibility fallback only.
      - Hono remains source of truth for Phoenix, SSH, LLM, safety, approvals, audit, SSE, activity, memory rules, and observability rules.
      - Retirement criteria for Vite: dashboard passes mock-mode smoke, dashboard run workflow covers ticket selection, run creation, SSE, approval edit/approve/reject, abort, activity draft/submit, dashboard contains no sample dashboard content, and no active milestone requirement points to Vite.

      Update docs/API.md to document GET /api/dashboard, its limit query, DashboardResponse fields, source labels, deferred memory and observability statuses, and the extended GET /api/runs/:runId response fields ticketId, customerSystemId, ticket, target, and source. Include that approval, activity, and SSE endpoints remain unchanged.

      Update docs/README.md with primary local commands: bun run dev:backend, bun run dev:frontend for dashboard on port 3000, and bun run dev:vite for fallback on port 5173.

      Update docs/LIMITATIONS.md to state that Postgres, pgvector, RAG memory implementation, observability instrumentation, and full v1.3 integration are deferred to later phases, and that Phase 1 memory/observability dashboard panels are read-only deferred statuses.
    </action>
    <acceptance_criteria>
      <criterion>docs/ARCHITECTURE.md states Next.js dashboard primary ownership and Vite fallback retirement criteria.</criterion>
      <criterion>docs/API.md documents GET /api/dashboard and extended GET /api/runs/:runId fields.</criterion>
      <criterion>docs/README.md lists dashboard port 3000 as the primary frontend path and Vite port 5173 as fallback.</criterion>
      <criterion>docs/LIMITATIONS.md does not claim Postgres, pgvector, RAG memory, observability instrumentation, live Phoenix, live SSH, sudo, or live LLM validation for Phase 1.</criterion>
      <criterion>Documentation points to the dashboard runtime/component test evidence from Plan 03 as the required proof that the main dashboard path has no sample records, fake metrics, fake charts, placeholder documents, throughput, conversion, revenue, or lorem content; source search may be recorded only as supplemental hygiene.</criterion>
    </acceptance_criteria>
  </task>

  <task id="04" type="auto">
    <name>Add final smoke and traceability verification</name>
    <read_first>
      <file>.planning/REQUIREMENTS.md</file>
      <file>package.json</file>
      <file>apps/dashboard/package.json</file>
      <file>docs/ARCHITECTURE.md</file>
      <file>docs/API.md</file>
      <file>apps/backend/src/routes/dashboard.ts</file>
      <file>apps/dashboard/app/dashboard/page.tsx</file>
      <file>apps/dashboard/app/dashboard/runs/[runId]/page.tsx</file>
    </read_first>
    <action>
      After implementation and verification commands pass, update .planning/REQUIREMENTS.md traceability statuses for DASH-01, DASH-02, DASH-03, and DASH-04 from Pending to Covered or Complete according to the repository's existing wording. Do not mark PG, VEC, MEM, OBS, or INT requirements covered in Phase 1.

      Record final verification evidence in the execution summary, including command results for bun run check and the sample-content guard. If docker compose build is run, record its result. If docker compose build cannot be run, record the exact environmental blocker without weakening the requirement.
    </action>
    <acceptance_criteria>
      <criterion>DASH-01, DASH-02, DASH-03, and DASH-04 statuses are updated only after implementation verification passes.</criterion>
      <criterion>No PG, VEC, MEM, OBS, or INT requirement status is changed by Phase 1.</criterion>
      <criterion>`bun run check` succeeds.</criterion>
      <criterion>The final verification summary includes passing dashboard runtime/component assertions from `bun run --filter techbold-track-dashboard test` covering rendered source labels, real sidebar routes, main-path actions, empty/error states, and absence of sample records, fake metrics, fake charts, placeholder documents, throughput, conversion, revenue, and lorem content.</criterion>
      <criterion>Manual smoke instructions are recorded: start `bun run dev:backend`, start `bun run dev:frontend`, open http://localhost:3000/dashboard, verify dashboard loads mock backend data, start a ticket run, navigate to /dashboard/runs/:runId, approve or reject through backend endpoints, and confirm activity state remains backend-owned.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run these commands after this plan:

- `bun install`
- `bun run --filter @techbold/contracts test`
- `bun run --filter @techbold/contracts typecheck`
- `bun run --filter techbold-track-backend test`
- `bun run --filter techbold-track-dashboard test`
- `bun run --filter techbold-track-dashboard typecheck`
- `bun run --filter techbold-track-dashboard build`
- `bun run --filter techbold-track-frontend test`
- `bun run --filter techbold-track-frontend typecheck`
- `bun run check`
- Runtime sample-content, source-label, sidebar-route, and main-path action assertions are required through `bun run --filter techbold-track-dashboard test`; source search may be recorded only as supplemental hygiene and is not pass/fail evidence.
- `docker compose build frontend`
</verification>
