---
phase: 01-dashboard-ownership-and-data-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/backend/src/app.ts
  - apps/backend/src/routes/dashboard.ts
  - apps/backend/src/routes/runs.ts
  - apps/backend/src/store/runs.ts
  - apps/backend/src/store/audit.ts
  - apps/backend/src/tests/dashboard.test.ts
  - apps/backend/src/tests/runs.test.ts
  - apps/backend/src/tests/app.test.ts
autonomous: true
requirements:
  - DASH-02
shared_requirements:
  - requirement: DASH-02
    reason: "Plan 02 implements the backend source for the typed contract created in Plan 01."

must_haves:
  truths:
    - "GET /api/dashboard returns dashboard data composed from Hono-owned APIs and store helpers, not frontend constants."
    - "GET /api/runs/:runId includes safe direct-navigation context so a dashboard URL can load a run without prior React component state."
    - "Dashboard aggregation is read-focused and does not alter Phoenix, SSH, model, approval, safety, audit, activity, or SSE behavior."
    - "Memory and observability are returned as explicit deferred statuses unless a real backend status exists."
    - "No endpoint leaks Phoenix tokens, SSH key paths, raw secret-bearing errors, or unredacted command output."
    - "The executor checked the dirty worktree before editing and did not revert unrelated user changes."
  artifacts:
    - path: "apps/backend/src/routes/dashboard.ts"
      provides: "GET /api/dashboard route that returns DashboardResponse."
    - path: "apps/backend/src/routes/runs.ts"
      provides: "Extended RunDetail response with ticketId, customerSystemId, ticket, target, and source."
    - path: "apps/backend/src/store/runs.ts"
      provides: "Read-only run summary helpers for dashboard aggregation."
    - path: "apps/backend/src/store/audit.ts"
      provides: "Read-only pending approval, audit evidence, and activity state summary helpers."
---

<objective>
Add the narrow backend dashboard data surface needed by the Next.js dashboard while preserving Hono as the source of truth for the existing run workflow.
</objective>

<context>
Read the phase source of truth before editing:

- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-CONTEXT.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-RESEARCH.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-PATTERNS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md
- AGENTS.md

Dirty worktree risk: run `git status --short` before editing. Do not revert unrelated user changes in .env.example, backend AI/env files, or apps/model files.
</context>

<tasks>
  <task id="01" type="auto">
    <name>Add read-only store summary helpers</name>
    <read_first>
      <file>apps/backend/src/store/runs.ts</file>
      <file>apps/backend/src/store/audit.ts</file>
      <file>apps/backend/src/store/schema.ts</file>
      <file>packages/contracts/src/runs.ts</file>
      <file>packages/contracts/src/dashboard.ts</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-PATTERNS.md</file>
    </read_first>
    <action>
      In apps/backend/src/store/runs.ts, add read-only helpers listRunSummaries(limit?: number) and parseSafeTarget(customerSystemId: string). listRunSummaries must query the runs table ordered by updated_at descending, parse each row with RunSchema, and return DashboardRunSummary-compatible objects with runId, ticketId, customerSystemId, status, phase, startedAt, updatedAt, completedAt, and target derived from customer_system_id when it is host:port. parseSafeTarget must return null for malformed input and must not include secrets.

      In apps/backend/src/store/audit.ts, add read-only helpers listPendingApprovalSummaries(limit?: number), listAuditEvidenceSummaries(limit?: number), and listActivityStateSummaries(limit?: number). They must query existing command_approvals, audit_events, activity_drafts, and runs tables only. They must parse rows with existing schemas where possible and return contract-compatible summaries. Do not add write paths or mutate status in these helpers.
    </action>
    <acceptance_criteria>
      <criterion>New helpers contain SELECT queries only and do not call db.run.</criterion>
      <criterion>listPendingApprovalSummaries returns only approvals with status "PENDING".</criterion>
      <criterion>listAuditEvidenceSummaries returns redacted payload summaries from existing audit_events.payload_json and does not expose raw thrown errors.</criterion>
      <criterion>parseSafeTarget("10.0.0.1:22") returns host "10.0.0.1" and port 22; parseSafeTarget("bad-value") returns null.</criterion>
      <criterion>`bun run --filter techbold-track-backend typecheck` succeeds after route changes are complete.</criterion>
    </acceptance_criteria>
  </task>

  <task id="02" type="auto">
    <name>Extend run detail for direct dashboard navigation</name>
    <read_first>
      <file>apps/backend/src/routes/runs.ts</file>
      <file>apps/backend/src/store/runs.ts</file>
      <file>apps/backend/src/store/audit.ts</file>
      <file>apps/backend/src/phoenix/client.ts</file>
      <file>apps/backend/src/phoenix/mock.ts</file>
      <file>packages/contracts/src/runs.ts</file>
      <file>apps/backend/src/tests/runs.test.ts</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-RESEARCH.md</file>
    </read_first>
    <action>
      Update GET /api/runs/:runId in apps/backend/src/routes/runs.ts so its JSON response matches RunDetail from @techbold/contracts. Keep existing fields runId, status, phase, timeline, pendingApproval, and activityDraft. Add ticketId from run.ticket_id, customerSystemId from run.customer_system_id, target from parseSafeTarget, source based on client mode ("mock-backend" when resolveClientMode("phoenix") is "mock", otherwise "live-backend"), and ticket as a safe summary when it can be loaded from getPhoenixClient().getTicket(run.ticket_id). If Phoenix lookup fails, return ticket: null and do not fail the run detail response.
    </action>
    <acceptance_criteria>
      <criterion>Existing run detail fields keep their current names and values.</criterion>
      <criterion>Direct GET /api/runs/:runId returns ticketId, customerSystemId, target, ticket, and source.</criterion>
      <criterion>Phoenix ticket lookup failure in run detail does not break approval, audit, activity, or SSE use for that run.</criterion>
      <criterion>apps/backend/src/tests/runs.test.ts includes assertions for the new fields and for absence of "token", "privateKey", "PHOENIX_API_TOKEN", and "/keys" in the response body.</criterion>
      <criterion>`bun run --filter techbold-track-backend test -- runs` succeeds.</criterion>
    </acceptance_criteria>
  </task>

  <task id="03" type="auto">
    <name>Add GET /api/dashboard aggregate route</name>
    <read_first>
      <file>apps/backend/src/app.ts</file>
      <file>apps/backend/src/routes/dashboard.ts</file>
      <file>apps/backend/src/routes/tickets.ts</file>
      <file>apps/backend/src/routes/health.ts</file>
      <file>apps/backend/src/routes/runs.ts</file>
      <file>apps/backend/src/store/db.ts</file>
      <file>apps/backend/src/store/runs.ts</file>
      <file>apps/backend/src/store/audit.ts</file>
      <file>packages/contracts/src/dashboard.ts</file>
      <file>packages/contracts/src/tickets.ts</file>
      <file>packages/contracts/src/runs.ts</file>
      <file>apps/backend/src/tests/dashboard.test.ts</file>
    </read_first>
    <action>
      Create apps/backend/src/routes/dashboard.ts exporting dashboardRouter. Mount it in apps/backend/src/app.ts as app.route("/api/dashboard", dashboardRouter).

      Implement GET /api/dashboard with optional query parameter limit parsed by Zod as an integer from 1 to 50 with default 20. The response must satisfy DashboardResponse from @techbold/contracts:

      - generatedAt: current ISO timestamp.
      - source: "mock-backend" when isMockMode() or Phoenix mock mode is active, otherwise "live-backend".
      - health: status "ok", backend mode from isMockMode(), and store from getStoreStatus().
      - tickets: from getPhoenixClient().listTickets({}) when available, safely mapped to ticket summaries. The dashboard response source is the authoritative backend-derived source label for these ticket summaries and any standalone ticket page that renders them.
      - runs: from listRunSummaries(limit).
      - pendingApprovals: from listPendingApprovalSummaries(limit).
      - auditEvidence: from listAuditEvidenceSummaries(limit).
      - activityStates: from listActivityStateSummaries(limit).
      - memory: deferred status with label "Deferred" and message "Memory evidence is deferred to Phase 3 and Phase 4."
      - observability: include backend health and deferred status with label "Deferred" and message "Operational signals are deferred to Phase 5."

      Handle PhoenixAuthError as 502 with { error: "upstream authentication failed" }, PhoenixNetworkError as 502 with { error: "ERP unavailable" }, and PhoenixNotFoundError as an empty tickets array. Do not expose raw error messages.
    </action>
    <acceptance_criteria>
      <criterion>GET /api/dashboard returns 200 in mock mode and includes health, tickets, runs, pendingApprovals, auditEvidence, activityStates, memory, and observability keys.</criterion>
      <criterion>apps/backend/src/tests/dashboard.test.ts asserts DashboardResponse.source is "mock-backend" or "live-backend" according to backend mode and is available for dashboard ticket source labeling.</criterion>
      <criterion>Memory and observability are read-only deferred statuses in Phase 1 and do not claim pgvector, RAG, traces, metrics, or live observability.</criterion>
      <criterion>The route contains no calls to advance, approve, reject, abort, draftActivity, submitActivity, SSH, model providers, or Phoenix mutation methods.</criterion>
      <criterion>apps/backend/src/tests/dashboard.test.ts covers mock-mode success, invalid limit 400, Phoenix network 502, deferred memory and observability labels, and no secret-looking fields in serialized output.</criterion>
      <criterion>`bun run --filter techbold-track-backend test -- dashboard` succeeds.</criterion>
    </acceptance_criteria>
  </task>

  <task id="04" type="auto">
    <name>Protect existing main-flow behavior with regression tests</name>
    <read_first>
      <file>apps/backend/src/tests/app.test.ts</file>
      <file>apps/backend/src/tests/runs.test.ts</file>
      <file>apps/backend/src/tests/approvals.test.ts</file>
      <file>apps/backend/src/tests/activity.test.ts</file>
      <file>apps/backend/src/routes/approvals.ts</file>
      <file>apps/backend/src/routes/activity.ts</file>
      <file>apps/backend/src/routes/events.ts</file>
      <file>apps/backend/src/events/sse.ts</file>
    </read_first>
    <action>
      Add focused regression assertions, without broad rewrites, proving the Phase 1 endpoint additions do not bypass existing run workflow behavior. app.test.ts should assert /api/dashboard is mounted. runs.test.ts should assert /api/runs/:runId/next still returns pendingApproval when the orchestrator reaches WAITING_FOR_APPROVAL. Existing approvals and activity tests should not be weakened. If a test fixture must change for the extended RunDetail response, update only the expected additional fields.
    </action>
    <acceptance_criteria>
      <criterion>`bun run --filter techbold-track-backend test -- app runs approvals activity` succeeds.</criterion>
      <criterion>Existing approve endpoint still uses editedCommand and returns 422 for commands blocked by the backend safety policy.</criterion>
      <criterion>Existing activity submit behavior still leaves tickets open when validation evidence is absent.</criterion>
      <criterion>Existing SSE stream remains available at GET /api/runs/:runId/events.</criterion>
      <criterion>`bun run --filter techbold-track-backend typecheck` succeeds.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run these commands after this plan:

- `bun run --filter techbold-track-backend test -- dashboard`
- `bun run --filter techbold-track-backend test -- runs`
- `bun run --filter techbold-track-backend test -- app runs approvals activity`
- `bun run --filter techbold-track-backend typecheck`
- `bun run --filter @techbold/contracts typecheck`
</verification>
