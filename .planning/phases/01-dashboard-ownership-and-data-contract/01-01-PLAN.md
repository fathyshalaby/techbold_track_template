---
phase: 01-dashboard-ownership-and-data-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/src/index.ts
  - packages/contracts/src/events.ts
  - packages/contracts/src/tickets.ts
  - packages/contracts/src/runs.ts
  - packages/contracts/src/dashboard.ts
  - packages/contracts/src/contracts.test.ts
  - apps/backend/package.json
  - apps/backend/src/events/sse.ts
  - apps/backend/src/tests/sse-audit-symmetry.test.ts
  - apps/frontend/package.json
  - apps/frontend/src/types.ts
  - apps/frontend/src/types.test.ts
autonomous: true
requirements:
  - DASH-02
shared_requirements:
  - requirement: DASH-02
    reason: "Plan 01 defines the typed data contract; Plan 02 implements the backend endpoint that serves it."

must_haves:
  truths:
    - "packages/contracts is an importable Bun workspace package named @techbold/contracts."
    - "SSE_EVENT_TYPES is defined once in packages/contracts/src/events.ts and imported by backend and Vite fallback code."
    - "Dashboard, ticket, run, approval, audit, activity, health, source label, memory status, and observability status response types are exported from @techbold/contracts."
    - "The contract package contains no Phoenix tokens, SSH key paths, command output samples, Acme records, fake metrics, or shadcn sample data."
    - "The executor checked the dirty worktree before editing and did not revert unrelated user changes."
  artifacts:
    - path: "packages/contracts/src/events.ts"
      provides: "Canonical SSE_EVENT_TYPES and SseEvent types."
    - path: "packages/contracts/src/dashboard.ts"
      provides: "DashboardResponse and source-label contracts for GET /api/dashboard."
    - path: "packages/contracts/src/runs.ts"
      provides: "Run detail, approval, audit, and activity contracts reused by Hono and dashboard code."
    - path: "apps/backend/src/events/sse.ts"
      provides: "Backend SSE stream that imports the shared event list without changing streaming behavior."
    - path: "apps/frontend/src/types.ts"
      provides: "Vite fallback re-exports or mirrors shared contracts during the temporary fallback period."
---

<objective>
Create the shared TypeScript contract package that Phase 1 implementations use for the dashboard, Hono endpoint, SSE events, and Vite fallback compatibility.
</objective>

<context>
Read the phase source of truth before editing:

- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-CONTEXT.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-RESEARCH.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-PATTERNS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md
- AGENTS.md

Dirty worktree risk: run `git status --short` before editing. Existing unrelated changes include .env.example, backend AI/env files, and apps/model files. Do not revert, overwrite, or normalize those changes unless the user explicitly asks.
</context>

<tasks>
  <task id="01" type="auto">
    <name>Create the contracts workspace package</name>
    <read_first>
      <file>package.json</file>
      <file>packages/contracts/agent-spec.md</file>
      <file>packages/contracts/api-contract.md</file>
      <file>apps/backend/package.json</file>
      <file>apps/frontend/package.json</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-CONTEXT.md</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-RESEARCH.md</file>
    </read_first>
    <action>
      Update root package.json workspaces to include "packages/contracts" without removing "apps/backend" or "apps/frontend". Create packages/contracts/package.json with name "@techbold/contracts", type "module", main "./src/index.ts", types "./src/index.ts", scripts "typecheck": "tsc --noEmit" and "test": "vitest run", and devDependencies for TypeScript and Vitest compatible with the current workspace. Create packages/contracts/tsconfig.json using strict TypeScript settings, module "NodeNext", moduleResolution "NodeNext", target "ES2022", noEmit true, and include ["src/**/*.ts"].
    </action>
    <acceptance_criteria>
      <criterion>package.json lists "packages/contracts" in workspaces and still lists "apps/backend" and "apps/frontend".</criterion>
      <criterion>packages/contracts/package.json exists and names the package "@techbold/contracts".</criterion>
      <criterion>packages/contracts/tsconfig.json uses strict TypeScript and noEmit.</criterion>
      <criterion>`bun install` completes without forcing React 19 or Next 16 into the workspace.</criterion>
      <criterion>`bun run --filter @techbold/contracts typecheck` succeeds after the contract source files from this plan are complete.</criterion>
    </acceptance_criteria>
  </task>

  <task id="02" type="auto">
    <name>Define canonical SSE, ticket, run, and dashboard contracts</name>
    <read_first>
      <file>packages/contracts/src/index.ts</file>
      <file>packages/contracts/src/events.ts</file>
      <file>packages/contracts/src/tickets.ts</file>
      <file>packages/contracts/src/runs.ts</file>
      <file>packages/contracts/src/dashboard.ts</file>
      <file>apps/frontend/src/types.ts</file>
      <file>apps/backend/src/events/sse.ts</file>
      <file>apps/backend/src/store/schema.ts</file>
      <file>apps/backend/src/phoenix/types.ts</file>
      <file>apps/backend/src/routes/health.ts</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md</file>
    </read_first>
    <action>
      Create packages/contracts/src/events.ts with the exact 15-entry SSE_EVENT_TYPES tuple currently duplicated in apps/backend/src/events/sse.ts and apps/frontend/src/types.ts, preserving the existing agent.unavailable event. Export SseEventType and SseEvent with payload: unknown.

      Create packages/contracts/src/tickets.ts exporting Ticket, TicketSummary, CustomerSystem, CustomerSystemResponse, SafeTarget, and SourceLabel. SourceLabel must be the union "live-backend" | "mock-backend" | "seed-data" | "deferred".

      Create packages/contracts/src/runs.ts exporting RiskLevel, CommandApprovalStatus, CommandApproval, ActivityDraft, AuditEvent, RunDetail, CreateRunResult, DashboardRunSummary, PendingApprovalSummary, AuditEvidenceSummary, and ActivityStateSummary. RunDetail must include existing fields runId, status, phase, timeline, pendingApproval, activityDraft plus safe direct-navigation context: ticketId, customerSystemId, ticket: TicketSummary | null, target: SafeTarget | null, and source: SourceLabel.

      Create packages/contracts/src/dashboard.ts exporting HealthSummary, DashboardMemoryStatus, DashboardObservabilityStatus, DashboardResponse, and DashboardDataSource. DashboardResponse must include generatedAt, source, health, tickets, runs, pendingApprovals, auditEvidence, activityStates, memory, and observability. Memory and observability contracts must support deferred read-only status with labels "Deferred" and copy matching the UI spec.

      Create packages/contracts/src/index.ts as a barrel export for events, tickets, runs, and dashboard.
    </action>
    <acceptance_criteria>
      <criterion>SSE_EVENT_TYPES has exactly these entries and order: run.started, agent.thought_summary, command.proposed, command.blocked, approval.required, command.executing, command.completed, observation.added, fix.proposed, validation.completed, activity.drafted, activity.submitted, agent.unavailable, run.completed, run.failed.</criterion>
      <criterion>RunDetail exposes ticketId, customerSystemId, ticket, target, and source in addition to the current run state fields.</criterion>
      <criterion>DashboardResponse contains no local-only metric fields such as throughput, revenue, conversion, fake trend, chart series, or sample document data.</criterion>
      <criterion>All exported source labels are typed, not freeform strings.</criterion>
      <criterion>`bun run --filter @techbold/contracts typecheck` succeeds.</criterion>
    </acceptance_criteria>
  </task>

  <task id="03" type="auto">
    <name>Consume the shared event contract in backend and Vite fallback</name>
    <read_first>
      <file>apps/backend/package.json</file>
      <file>apps/backend/src/events/sse.ts</file>
      <file>apps/backend/src/tests/sse-audit-symmetry.test.ts</file>
      <file>apps/frontend/package.json</file>
      <file>apps/frontend/src/types.ts</file>
      <file>apps/frontend/src/types.test.ts</file>
      <file>packages/contracts/src/events.ts</file>
      <file>packages/contracts/src/runs.ts</file>
    </read_first>
    <action>
      Add "@techbold/contracts": "workspace:*" to apps/backend/package.json and apps/frontend/package.json dependencies. Update apps/backend/src/events/sse.ts to import SSE_EVENT_TYPES and SseEventType from "@techbold/contracts" while preserving createSseStream behavior, backfill writes, redaction, keepalive, and runEventBus subscription behavior. Update apps/frontend/src/types.ts to re-export shared SSE and core response types from "@techbold/contracts" or to import them and export compatible aliases for the Vite fallback.

      Add or update tests so apps/backend/src/tests/sse-audit-symmetry.test.ts or a focused contract test asserts the backend SSE_EVENT_TYPES import equals the contracts tuple. Update apps/frontend/src/types.test.ts to assert the Vite fallback SSE_EVENT_TYPES equals the contracts tuple.
    </action>
    <acceptance_criteria>
      <criterion>Backend and Vite fallback tests import SSE_EVENT_TYPES through their public modules and assert the runtime tuple equals @techbold/contracts, proving both paths consume the shared contract instead of a disconnected local event list.</criterion>
      <criterion>`bun run --filter techbold-track-backend test -- sse-audit-symmetry` succeeds or the equivalent focused backend SSE contract test succeeds.</criterion>
      <criterion>`bun run --filter techbold-track-frontend test -- types` succeeds.</criterion>
      <criterion>`bun run --filter techbold-track-backend typecheck` succeeds.</criterion>
      <criterion>`bun run --filter techbold-track-frontend typecheck` succeeds.</criterion>
    </acceptance_criteria>
  </task>

  <task id="04" type="auto">
    <name>Add contract package verification</name>
    <read_first>
      <file>packages/contracts/src/contracts.test.ts</file>
      <file>packages/contracts/src/events.ts</file>
      <file>packages/contracts/src/dashboard.ts</file>
      <file>packages/contracts/src/runs.ts</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md</file>
    </read_first>
    <action>
      Create packages/contracts/src/contracts.test.ts with Vitest assertions for the canonical SSE tuple, SourceLabel values, deferred memory status shape, deferred observability status shape, and RunDetail direct-navigation fields. The test must not use placeholder Acme, sample, fake, lorem, or decorative dashboard content.
    </action>
    <acceptance_criteria>
      <criterion>`bun run --filter @techbold/contracts test` succeeds.</criterion>
      <criterion>`bun run --filter @techbold/contracts typecheck` succeeds.</criterion>
      <criterion>packages/contracts/src/contracts.test.ts builds representative DashboardResponse and RunDetail fixtures from backend-shaped data, serializes them, and asserts they expose only typed source/deferred labels with no sample records, fake operational metrics, placeholder copy, throughput, conversion, or revenue fields.</criterion>
      <criterion>`bun run check` succeeds after all plans in Phase 1 are implemented.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run these commands after this plan:

- `bun install`
- `bun run --filter @techbold/contracts test`
- `bun run --filter @techbold/contracts typecheck`
- `bun run --filter techbold-track-backend test -- sse-audit-symmetry`
- `bun run --filter techbold-track-frontend test -- types`
- `bun run --filter techbold-track-backend typecheck`
- `bun run --filter techbold-track-frontend typecheck`
</verification>
