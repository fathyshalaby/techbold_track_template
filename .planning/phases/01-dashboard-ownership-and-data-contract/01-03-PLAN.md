---
phase: 01-dashboard-ownership-and-data-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - apps/dashboard/package.json
  - apps/dashboard/tsconfig.json
  - apps/dashboard/next.config.ts
  - apps/dashboard/postcss.config.mjs
  - apps/dashboard/components.json
  - apps/dashboard/app/layout.tsx
  - apps/dashboard/app/globals.css
  - apps/dashboard/app/page.tsx
  - apps/dashboard/app/dashboard/page.tsx
  - apps/dashboard/app/dashboard/tickets/page.tsx
  - apps/dashboard/app/dashboard/tickets/[ticketId]/page.tsx
  - apps/dashboard/app/dashboard/runs/page.tsx
  - apps/dashboard/app/dashboard/runs/[runId]/page.tsx
  - apps/dashboard/app/dashboard/approvals/page.tsx
  - apps/dashboard/app/dashboard/audit/page.tsx
  - apps/dashboard/app/dashboard/activity/page.tsx
  - apps/dashboard/app/dashboard/memory/page.tsx
  - apps/dashboard/app/dashboard/observability/page.tsx
  - apps/dashboard/app/dashboard/backend-status/page.tsx
  - apps/dashboard/components/app-sidebar.tsx
  - apps/dashboard/components/backend-status.tsx
  - apps/dashboard/components/dashboard-shell.tsx
  - apps/dashboard/components/run-workflow.tsx
  - apps/dashboard/components/ticket-table.tsx
  - apps/dashboard/components/ui/badge.tsx
  - apps/dashboard/components/ui/button.tsx
  - apps/dashboard/components/ui/card.tsx
  - apps/dashboard/components/ui/dialog.tsx
  - apps/dashboard/components/ui/input.tsx
  - apps/dashboard/components/ui/sheet.tsx
  - apps/dashboard/components/ui/skeleton.tsx
  - apps/dashboard/components/ui/table.tsx
  - apps/dashboard/components/ui/textarea.tsx
  - apps/dashboard/components/ui/tooltip.tsx
  - apps/dashboard/lib/api.ts
  - apps/dashboard/lib/events.ts
  - apps/dashboard/lib/source-labels.ts
  - apps/dashboard/lib/utils.ts
  - apps/dashboard/app/dashboard/dashboard.test.tsx
  - apps/dashboard/vitest.config.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-03

must_haves:
  truths:
    - "apps/dashboard is a Next.js App Router package using Next.js 15.5.19 and React 18.3.1 unless package resolution makes that impossible."
    - "The dashboard first screen is /dashboard and shows tickets, runs, approvals, audit evidence, activity state, memory status, observability status, and backend status from backend APIs or explicit deferred labels."
    - "The dashboard shell is source-owned service desk UI, not shadcn sample content."
    - "Run detail preserves existing endpoints for /next, approve, reject, abort, activity draft, activity submit, and direct EventSource SSE."
    - "No dashboard main-path action is inert: every button calls a backend endpoint, navigates to a real route, changes meaningful local state, or is omitted."
    - "The executor checked the dirty worktree before editing and did not revert unrelated user changes."
  artifacts:
    - path: "apps/dashboard/package.json"
      provides: "Next.js dashboard workspace package."
    - path: "apps/dashboard/app/dashboard/page.tsx"
      provides: "Operational overview backed by GET /api/dashboard."
    - path: "apps/dashboard/app/dashboard/runs/page.tsx"
      provides: "Run queue route backed by GET /api/dashboard run summaries."
    - path: "apps/dashboard/app/dashboard/runs/[runId]/page.tsx"
      provides: "Run workflow route preserving safety, approval, SSE, audit, and activity behavior."
    - path: "apps/dashboard/app/dashboard/backend-status/page.tsx"
      provides: "Backend status route backed by dashboard health and source data."
    - path: "apps/dashboard/lib/api.ts"
      provides: "Typed Hono API client using NEXT_PUBLIC_API_BASE."
    - path: "apps/dashboard/components/run-workflow.tsx"
      provides: "Client-side run controls and EventSource handling."
---

<objective>
Create the Next.js dashboard package and source-owned shadcn-based dashboard shell that becomes the target primary operational UI path for Phase 1.
</objective>

<context>
Read the phase source of truth before editing:

- .planning/STATE.md
- .planning/ROADMAP.md
- .planning/REQUIREMENTS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-CONTEXT.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-RESEARCH.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-PATTERNS.md
- .planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md
- AGENTS.md

Version decision: use Next.js 15.5.19 with React 18.3.1 for Phase 1. If implementation-time package resolution proves impossible, make the narrowest compatible adjustment and document the exact reason. Do not plan or perform a broad React 19 or Next 16 migration in this phase.

Dirty worktree risk: run `git status --short` before editing. Do not revert unrelated user changes.
</context>

<tasks>
  <task id="01" type="auto">
    <name>Create the Next.js dashboard package and shadcn foundation</name>
    <read_first>
      <file>package.json</file>
      <file>apps/frontend/package.json</file>
      <file>apps/frontend/src/App.tsx</file>
      <file>apps/dashboard/package.json</file>
      <file>apps/dashboard/components.json</file>
      <file>apps/dashboard/app/globals.css</file>
      <file>packages/contracts/package.json</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md</file>
    </read_first>
    <action>
      Create apps/dashboard as a new workspace package named "techbold-track-dashboard". Its package.json must include scripts "dev": "next dev --hostname 0.0.0.0 --port 3000", "build": "next build", "start": "next start --hostname 0.0.0.0 --port 3000", "typecheck": "tsc --noEmit", "test": "vitest run", and "lint": "biome check .". Add dependencies "next": "15.5.19", "react": "18.3.1", "react-dom": "18.3.1", "@techbold/contracts": "workspace:*", "lucide-react", "class-variance-authority", "clsx", "tailwind-merge", "@radix-ui/react-dialog", and "@radix-ui/react-tooltip". Add compatible devDependencies for TypeScript, @types/react, @types/react-dom, vitest, @testing-library/react, @testing-library/jest-dom, jsdom, tailwindcss, postcss, and autoprefixer.

      Create tsconfig.json, next.config.ts, postcss.config.mjs, components.json, app/layout.tsx, app/globals.css, and lib/utils.ts. Initialize shadcn according to the official Next.js layout and source-own the components under apps/dashboard/components/ui. Use the UI spec palette, spacing, and typography. Use lucide-react icons in navigation and icon buttons. Do not import any third-party registry other than official shadcn components.
    </action>
    <acceptance_criteria>
      <criterion>apps/dashboard/package.json pins next to "15.5.19" and react/react-dom to "18.3.1" unless a documented package-resolution note is added in the plan execution summary.</criterion>
      <criterion>apps/dashboard/components.json exists and points to source-owned components in apps/dashboard/components/ui.</criterion>
      <criterion>apps/dashboard/app/globals.css uses the UI spec colors #f8fafc, #ffffff, #d9e0e8, #111827, #667085, #2563eb, #15803d, #b45309, and #b91c1c or equivalent CSS variables.</criterion>
      <criterion>`bun run --filter techbold-track-dashboard typecheck` succeeds after app files are complete.</criterion>
      <criterion>The dashboard test suite renders the root shell and overview from backend-shaped fixtures and asserts only service-desk content, backend source labels, and deferred statuses are visible, with no shadcn sample team, fake chart, fake metric, placeholder document, or lorem content in the rendered output.</criterion>
    </acceptance_criteria>
  </task>

  <task id="02" type="auto">
    <name>Build the typed dashboard API and EventSource clients</name>
    <read_first>
      <file>apps/dashboard/lib/api.ts</file>
      <file>apps/dashboard/lib/events.ts</file>
      <file>apps/dashboard/lib/source-labels.ts</file>
      <file>packages/contracts/src/index.ts</file>
      <file>apps/frontend/src/App.tsx</file>
      <file>apps/backend/src/routes/dashboard.ts</file>
      <file>apps/backend/src/routes/runs.ts</file>
      <file>apps/backend/src/routes/approvals.ts</file>
      <file>apps/backend/src/routes/activity.ts</file>
      <file>apps/backend/src/routes/events.ts</file>
    </read_first>
    <action>
      Create apps/dashboard/lib/api.ts with a typed API client that reads NEXT_PUBLIC_API_BASE and defaults to "http://localhost:8000". Export API_BASE, dashboardUrl helpers, getDashboard(limit?: number), listTickets(params?), getTicket(ticketId), getCustomerSystem(ticketId), createRun(ticketId), getRun(runId), advanceRun(runId), abortRun(runId), approveCommand(runId, approvalId, body), rejectCommand(runId, approvalId, reason), draftActivity(runId), and submitActivity(runId, fields). The client must surface backend { error } messages as Error.message and must not swallow 422 safety errors.

      Create apps/dashboard/lib/events.ts with getRunEventsUrl(runId) and subscribeToRunEvents(runId, handlers) that uses direct browser EventSource against `${API_BASE}/api/runs/${runId}/events` and registers listeners for every SSE_EVENT_TYPES entry from @techbold/contracts. Do not proxy SSE through Next.js in Phase 1.

      Create apps/dashboard/lib/source-labels.ts with typed UI labels for "live-backend", "mock-backend", "seed-data", and "deferred". Use labels "Live backend", "Mock backend", "Seed data", and "Deferred".
    </action>
    <acceptance_criteria>
      <criterion>No file in apps/dashboard inlines "http://localhost:8000" except apps/dashboard/lib/api.ts.</criterion>
      <criterion>approveCommand sends editedCommand when provided and does not locally classify or execute commands.</criterion>
      <criterion>rejectCommand sends a non-empty reason and propagates backend validation errors.</criterion>
      <criterion>subscribeToRunEvents imports SSE_EVENT_TYPES from @techbold/contracts and registers all 14 event names.</criterion>
      <criterion>`bun run --filter techbold-track-dashboard typecheck` succeeds.</criterion>
    </acceptance_criteria>
  </task>

  <task id="03" type="auto">
    <name>Implement dashboard routes and source-owned operational surfaces</name>
    <read_first>
      <file>apps/dashboard/app/layout.tsx</file>
      <file>apps/dashboard/app/page.tsx</file>
      <file>apps/dashboard/app/dashboard/page.tsx</file>
      <file>apps/dashboard/app/dashboard/tickets/page.tsx</file>
      <file>apps/dashboard/app/dashboard/tickets/[ticketId]/page.tsx</file>
      <file>apps/dashboard/app/dashboard/runs/page.tsx</file>
      <file>apps/dashboard/app/dashboard/approvals/page.tsx</file>
      <file>apps/dashboard/app/dashboard/audit/page.tsx</file>
      <file>apps/dashboard/app/dashboard/activity/page.tsx</file>
      <file>apps/dashboard/app/dashboard/memory/page.tsx</file>
      <file>apps/dashboard/app/dashboard/observability/page.tsx</file>
      <file>apps/dashboard/app/dashboard/backend-status/page.tsx</file>
      <file>apps/dashboard/components/app-sidebar.tsx</file>
      <file>apps/dashboard/components/backend-status.tsx</file>
      <file>apps/dashboard/components/dashboard-shell.tsx</file>
      <file>apps/dashboard/components/ticket-table.tsx</file>
      <file>apps/dashboard/lib/api.ts</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md</file>
    </read_first>
    <action>
      Implement the App Router pages:

      - app/page.tsx redirects to /dashboard.
      - app/dashboard/page.tsx loads getDashboard() and renders backend health, source badge, ticket queue summary, run summaries, pending approvals, audit evidence, activity states, memory deferred status, and observability health/deferred status.
      - app/dashboard/tickets/page.tsx loads getDashboard() and renders ticket summaries, filters, backend-derived source labels from DashboardResponse.source, and Start run actions.
      - app/dashboard/tickets/[ticketId]/page.tsx loads getDashboard() for the backend-derived source label and matching ticket summary, then loads GET /api/tickets/:id and GET /api/tickets/:id/customer-system for ticket detail and safe target metadata. It calls createRun before navigating to /dashboard/runs/:runId and must not substitute a local source default if the dashboard source is unavailable.
      - app/dashboard/runs/page.tsx reads run summaries from getDashboard(), renders source labels from DashboardResponse.source, and links each run to /dashboard/runs/:runId.
      - app/dashboard/approvals/page.tsx reads pending approvals from getDashboard() and links each approval to /dashboard/runs/:runId.
      - app/dashboard/audit/page.tsx reads auditEvidence from getDashboard() and renders timestamp, actor, event type, run link, and redacted payload summary.
      - app/dashboard/activity/page.tsx reads activityStates from getDashboard() and links each state to /dashboard/runs/:runId.
      - app/dashboard/memory/page.tsx renders the backend memory status from getDashboard(), which is deferred in Phase 1.
      - app/dashboard/observability/page.tsx renders /health-backed status and the deferred Phase 5 operational signal status from getDashboard().
      - app/dashboard/backend-status/page.tsx renders backend health, mode, store status, source, memory deferred status, and observability deferred status from getDashboard().

      Implement DashboardShell with sidebar navigation labels Tickets, Runs, Approvals, Audit, Activity, Memory, Observability, and Backend Status. The first screen must be an operational dashboard, not a landing page. Every visible button must call a backend action, navigate to one of the routes above, refresh data, or be omitted.
    </action>
    <acceptance_criteria>
      <criterion>/dashboard renders tickets, runs, approvals, audit evidence, activity state, memory status, observability status, and backend health surfaces.</criterion>
      <criterion>Sidebar navigation labels map to real routes with rendered pages: /dashboard/tickets, /dashboard/runs, /dashboard/approvals, /dashboard/audit, /dashboard/activity, /dashboard/memory, /dashboard/observability, and /dashboard/backend-status.</criterion>
      <criterion>Every displayed surface includes a source label derived from backend data or deferred status.</criterion>
      <criterion>Ticket list and ticket detail source labels are derived from getDashboard() response data; component tests fail if those routes fall back to frontend-selected source constants.</criterion>
      <criterion>Memory surface uses "Memory evidence is deferred to Phase 3 and Phase 4." unless the backend contract provides a real status.</criterion>
      <criterion>Observability surface uses "Operational signals are deferred to Phase 5." unless the backend contract provides a real status.</criterion>
      <criterion>Component tests render /dashboard, /dashboard/tickets, /dashboard/runs, and /dashboard/backend-status with backend-shaped fixtures and assert no sample records, fake operational metrics, placeholder copy, throughput, conversion, or revenue content appears in the rendered output.</criterion>
      <criterion>`bun run --filter techbold-track-dashboard test` succeeds after tests are complete.</criterion>
    </acceptance_criteria>
  </task>

  <task id="04" type="auto">
    <name>Implement run detail workflow without bypassing backend safety</name>
    <read_first>
      <file>apps/dashboard/app/dashboard/runs/[runId]/page.tsx</file>
      <file>apps/dashboard/components/run-workflow.tsx</file>
      <file>apps/dashboard/lib/api.ts</file>
      <file>apps/dashboard/lib/events.ts</file>
      <file>apps/frontend/src/App.tsx</file>
      <file>apps/backend/src/routes/runs.ts</file>
      <file>apps/backend/src/routes/approvals.ts</file>
      <file>apps/backend/src/routes/activity.ts</file>
      <file>apps/backend/src/routes/events.ts</file>
      <file>docs/SAFETY_POLICY.md</file>
    </read_first>
    <action>
      Implement /dashboard/runs/[runId]/page.tsx to server-load getRun(runId), then render a client component RunWorkflow. RunWorkflow must preserve the existing Vite behavior:

      - Subscribe to direct Hono SSE through subscribeToRunEvents(runId).
      - Refresh run state on event receipt and on user action completion.
      - Advance run through POST /api/runs/:runId/next only.
      - Approve through POST /api/runs/:runId/approvals/:approvalId/approve with editedCommand when the technician edits the command.
      - Reject through POST /api/runs/:runId/approvals/:approvalId/reject with a visible reason input.
      - Abort through POST /api/runs/:runId/abort after a shadcn dialog confirmation.
      - Draft and submit activity through existing activity endpoints.
      - Show pending approvals prominently before secondary evidence panels.
      - Show SSE disconnected state near live events.

      Do not add any SSH execution, Phoenix mutation, safety classification, model call, memory write, or audit write in Next.js code.
    </action>
    <acceptance_criteria>
      <criterion>RunWorkflow contains no import from backend internals and no direct calls to SSH, Phoenix, model, safety, store, or audit modules.</criterion>
      <criterion>Approval controls call approveCommand and rejectCommand from apps/dashboard/lib/api.ts.</criterion>
      <criterion>Edited commands are sent to the backend as editedCommand and backend 422 errors remain visible in the approval panel.</criterion>
      <criterion>Activity draft and submit controls call draftActivity and submitActivity from apps/dashboard/lib/api.ts.</criterion>
      <criterion>EventSource subscriptions use /api/runs/:runId/events and all canonical SSE event names.</criterion>
      <criterion>`bun run --filter techbold-track-dashboard typecheck` succeeds.</criterion>
    </acceptance_criteria>
  </task>

  <task id="05" type="auto">
    <name>Add focused dashboard tests</name>
    <read_first>
      <file>apps/dashboard/vitest.config.ts</file>
      <file>apps/dashboard/app/dashboard/dashboard.test.tsx</file>
      <file>apps/dashboard/app/dashboard/runs/page.tsx</file>
      <file>apps/dashboard/app/dashboard/backend-status/page.tsx</file>
      <file>apps/dashboard/components/run-workflow.tsx</file>
      <file>apps/dashboard/components/ticket-table.tsx</file>
      <file>apps/dashboard/lib/source-labels.ts</file>
      <file>packages/contracts/src/dashboard.ts</file>
      <file>.planning/phases/01-dashboard-ownership-and-data-contract/01-UI-SPEC.md</file>
    </read_first>
    <action>
      Add Vitest and Testing Library coverage for dashboard data mapping and main-path safeguards. Tests must cover:

      - source label rendering for Live backend, Mock backend, Seed data, and Deferred.
      - empty ticket, empty run, and empty pending approval states.
      - backend error message rendering using "Could not load dashboard data. Check backend status and retry."
      - a Start run action calls createRun and navigates to /dashboard/runs/:runId.
      - run workflow approval calls approveCommand with editedCommand and never calls a local command execution function.
      - runtime content guard: rendered dashboard fixtures for overview, ticket list, run queue, and backend status do not include shadcn sample team text, fake metric text, fake chart text, placeholder document text, throughput, conversion, revenue, or lorem text.
      - sidebar route coverage: every sidebar item has a real href, a corresponding page file, and a navigation assertion.
      - ticket source coverage: ticket list and ticket detail render source labels from getDashboard() fixture values and fail when only a frontend fallback label is available.
    </action>
    <acceptance_criteria>
      <criterion>`bun run --filter techbold-track-dashboard test` succeeds.</criterion>
      <criterion>`bun run --filter techbold-track-dashboard typecheck` succeeds.</criterion>
      <criterion>`bun run --filter techbold-track-dashboard build` succeeds.</criterion>
      <criterion>apps/dashboard/app/dashboard/dashboard.test.tsx renders backend-shaped dashboard fixtures and asserts source labels, route links, actions, empty/error states, and runtime absence of sample records, fake metrics, fake charts, placeholder documents, throughput, conversion, revenue, and lorem content.</criterion>
    </acceptance_criteria>
  </task>
</tasks>

<verification>
Run these commands after this plan:

- `bun install`
- `bun run --filter techbold-track-dashboard test`
- `bun run --filter techbold-track-dashboard typecheck`
- `bun run --filter techbold-track-dashboard build`
- Runtime sample-content, source-label, and route-coverage assertions are required through `bun run --filter techbold-track-dashboard test`; source search may be recorded only as supplemental hygiene and is not pass/fail evidence.
</verification>
