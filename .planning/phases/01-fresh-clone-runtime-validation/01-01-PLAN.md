---
status: ready
---

# 01-01 Fresh-Clone Runtime Validation

## Trace

- B-003
- BR-002
- T-001
- P-002
- A-001

## Requirements

- LIVE-01
- LIVE-02

## Tasks

- Create a temporary local clone from the current committed repository state.
- In the clone, copy `.env.example` to `.env` and run `docker compose up --build`.
- Check backend `/health` and frontend `http://localhost:5173` if the stack starts.
- Record exact command output evidence, exit codes, and any environment blockers.
- Update README or infrastructure docs only if observed behavior contradicts the documented setup path.

## Verification

- `git status --short`
- `docker compose version`
- `cp .env.example .env`
- `docker compose up --build`
- `curl -fsS http://localhost:8000/health`
- `curl -fsS http://localhost:5173`
