---
phase: 04-ssh-executor
plan: "04"
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/src/ssh/mock.ts
  - backend/src/tests/ssh-mock.test.ts
autonomous: true
requirements:
  - DIAG-06
must_haves:
  truths:
    - "SSH mock drives the full agent loop offline without a real VM"
    - "Mock exposes identical executeApprovedCommand signature and result shape as real executor"
    - "Exact-command lookup returns the scripted fixture; unmatched command returns default fallback"
    - "Mock preflight always returns sudoAvailable:true, langIsC:true, PATH present"
    - "Default fixture map covers a complete diagnose→fix→validate practice loop sequence"
  artifacts:
    - path: backend/src/ssh/mock.ts
      provides: "MockSshExecutor class, createMockSshExecutor factory, MOCK_SSH_FIXTURES map"
      exports:
        - MockSshExecutor
        - createMockSshExecutor
        - MOCK_SSH_FIXTURES
        - DEFAULT_FALLBACK_RESULT
    - path: backend/src/tests/ssh-mock.test.ts
      provides: "TDD suite: fixture lookup, fallback, preflight, interface conformance"
  key_links:
    - from: backend/src/ssh/mock.ts
      to: backend/src/ssh/types.ts
      via: "import { CommandResult, PreflightResult, SshExecutor, SshTarget }"
      pattern: "SshExecutor"
---

<objective>
TDD: write failing tests for the SSH mock, then implement mock.ts to make them green.

Purpose: The mock is the primary enabler of offline demo and CI. It must implement SshExecutor
identically to the real executor so the orchestrator (Phase 5) can swap implementations via
resolveClientMode without any other code change. Rich fixtures covering a realistic
diagnose→fix→validate sequence are what make the offline practice loop credible to judges.

Output: `backend/src/tests/ssh-mock.test.ts` (RED → GREEN) and `backend/src/ssh/mock.ts`.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/phases/04-ssh-executor/04-CONTEXT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/ssh/types.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/phoenix/mock.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/docs/ARCHITECTURE.md
</context>

<feature>
  <name>MockSshExecutor — scripted SSH responses for offline demo and CI</name>
  <files>backend/src/ssh/mock.ts, backend/src/tests/ssh-mock.test.ts</files>
  <behavior>
    RED tests to write first (ssh-mock.test.ts):

    1. Fixture lookup — exact command match:
       executeApprovedCommand('uname -a', target) returns result where stdout contains
       'Linux' and exitCode === 0 and timedOut === false and durationMs >= 0.

    2. Fixture lookup — systemctl status:
       executeApprovedCommand('systemctl status status-api --no-pager', target) returns
       stdout containing 'active' or 'failed' (non-empty), exitCode is a number.

    3. Fallback for unknown command:
       executeApprovedCommand('some-unknown-command-xyz', target) returns DEFAULT_FALLBACK_RESULT
       shape: exitCode === 0, stdout is a non-empty string, timedOut === false.

    4. Result shape conformance:
       Every result from executeApprovedCommand has exactly the 5 fields: exitCode (number|null),
       stdout (string), stderr (string), durationMs (number), timedOut (boolean). No extra fields.

    5. Preflight always succeeds:
       runPreflight(runId, target) returns { sudoAvailable: true, langIsC: true, path: string }
       where path is non-empty.

    6. Preflight is idempotent:
       runPreflight called twice with the same runId returns the same object reference (cached).

    7. SshExecutor interface conformance:
       createMockSshExecutor() returns an object with executeApprovedCommand and runPreflight
       methods (typeof === 'function').

    8. Practice loop coverage — fix cycle:
       'sudo systemctl restart status-api' returns exitCode === 0.
       'sudo systemctl enable status-api' returns exitCode === 0.

    9. Practice loop coverage — validation:
       'systemctl is-active status-api' returns stdout.trim() === 'active'.
       'curl -s -o /dev/null -w "%{http_code}" localhost:8080' returns stdout containing '200'.

    10. Anti-pattern A1 guard:
        The test file asserts (via a static string check on the mock module source or a
        deliberate import-guard comment) that executeApprovedCommand in mock.ts matches the
        SshExecutor interface and is NOT re-exported from any ai/tools/ file.
        Implement as: import { executeApprovedCommand } from '../ssh/mock.js' resolves cleanly;
        separate grep assertion that ai/tools/ssh-tools.ts does not import from '../ssh/mock'.
  </behavior>
  <implementation>
    After all tests are RED, implement backend/src/ssh/mock.ts:

    Import CommandResult, PreflightResult, SshExecutor, SshTarget from './types.js'.

    MOCK_SSH_FIXTURES: Record&lt;string, CommandResult&gt; — a plain object (not a Map) keyed by
    exact command string. Include at minimum these fixture keys and realistic scripted values:

    - 'uname -a':
      stdout: 'Linux vm-01 5.15.0-1034-azure #41-Ubuntu SMP Fri Feb 10 19:59:48 UTC 2023 x86_64 x86_64 x86_64 GNU/Linux'
      stderr: '', exitCode: 0, durationMs: 42, timedOut: false

    - 'systemctl status status-api --no-pager':
      stdout: '● status-api.service - Status API\n   Loaded: loaded (/etc/systemd/system/status-api.service; enabled)\n   Active: failed (Result: exit-code)\n  Process: 1234 ExecStart=/usr/bin/node /srv/status-api/index.js (code=exited, status=1/FAILURE)\n'
      stderr: '', exitCode: 3, durationMs: 55, timedOut: false

    - 'journalctl -u status-api -n 100 --no-pager':
      stdout: 'Jun 06 08:00:01 vm-01 node[1234]: Error: EADDRINUSE: address already in use :::8080\nJun 06 08:00:01 vm-01 systemd[1]: status-api.service: Main process exited, code=exited, status=1/FAILURE\n'
      stderr: '', exitCode: 0, durationMs: 61, timedOut: false

    - 'ss -tulpn':
      stdout: 'Netid State  Recv-Q Send-Q Local Address:Port  Peer Address:Port\ntcp   LISTEN 0      511    0.0.0.0:8080       0.0.0.0:*       users:(("node",pid=999,fd=6))\n'
      stderr: '', exitCode: 0, durationMs: 38, timedOut: false

    - 'sudo kill -9 999' (stale process cleanup):
      stdout: '', stderr: '', exitCode: 0, durationMs: 44, timedOut: false

    - 'sudo systemctl restart status-api':
      stdout: '', stderr: '', exitCode: 0, durationMs: 120, timedOut: false

    - 'sudo systemctl enable status-api':
      stdout: 'Created symlink /etc/systemd/system/multi-user.target.wants/status-api.service → /etc/systemd/system/status-api.service.'
      stderr: '', exitCode: 0, durationMs: 55, timedOut: false

    - 'systemctl is-active status-api':
      stdout: 'active', stderr: '', exitCode: 0, durationMs: 33, timedOut: false

    - 'curl -s -o /dev/null -w "%{http_code}" localhost:8080':
      stdout: '200', stderr: '', exitCode: 0, durationMs: 88, timedOut: false

    - 'df -h':
      stdout: 'Filesystem      Size  Used Avail Use% Mounted on\n/dev/sda1        30G   8.1G   20G  29% /\n'
      stderr: '', exitCode: 0, durationMs: 28, timedOut: false

    - 'free -m':
      stdout: '              total        used        free      shared  buff/cache   available\nMem:           7986        1234        5421          42        1330        6509\nSwap:          2047           0        2047\n'
      stderr: '', exitCode: 0, durationMs: 25, timedOut: false

    DEFAULT_FALLBACK_RESULT: CommandResult = {
      stdout: 'mock: command executed (no fixture defined)',
      stderr: '', exitCode: 0, durationMs: 10, timedOut: false
    }

    export class MockSshExecutor implements SshExecutor:
      - private preflightCache = new Map&lt;string, PreflightResult&gt;()
      - async executeApprovedCommand(command: string, _target: SshTarget): Promise&lt;CommandResult&gt;
        returns MOCK_SSH_FIXTURES[command] ?? DEFAULT_FALLBACK_RESULT (wrapped in Promise.resolve)
      - async runPreflight(runId: string, _target: SshTarget): Promise&lt;PreflightResult&gt;
        checks cache; if missing builds { sudoAvailable: true, langIsC: true, path: '/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin' } and caches it.

    export function createMockSshExecutor(): SshExecutor — returns new MockSshExecutor().

    The mock module MUST NOT import anything from ssh2. It is pure TypeScript, no I/O.
    Comment at top of file: // Mock SSH executor — no ssh2 dependency. Used for offline demo and CI.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mock → test suite | All fixture data is in-code, no external I/O, no secrets |
| mock → orchestrator | Identical SshExecutor interface — orchestrator cannot distinguish mock from real at the type level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04M-FIX | Information Disclosure | Fixture data contains real-looking system info | accept | All fixture data is synthetic (fictional IPs, no real credentials); no real VM data committed |
| T-04M-A1 | Elevation of Privilege | Mock executeApprovedCommand mistakenly registered as model tool | mitigate | Test 10 (A1 guard) asserts mock is not imported in ai/tools/ssh-tools.ts; test must stay green after 04-05 |
| T-04M-SC | Tampering | npm/pip/cargo installs | accept | No new packages — mock uses only TypeScript and types already in place |
</threat_model>

<verification>
- RED: vitest run ssh-mock.test.ts fails before mock.ts is implemented
- GREEN: vitest run ssh-mock.test.ts exits 0 after mock.ts implementation
- npx tsc --noEmit: no errors
- grep "import.*ssh2" backend/src/ssh/mock.ts: empty (mock has no ssh2 dependency)
</verification>

<success_criteria>
1. All 10 behavioral tests are GREEN after implementation
2. MOCK_SSH_FIXTURES covers the full diagnose→fix→validate practice loop (uname, systemctl status, journalctl, ss, restart, enable, is-active, curl)
3. createMockSshExecutor() returns a valid SshExecutor — orchestrator can substitute it for the real executor without any other code change
4. Mock has zero ssh2 dependency
</success_criteria>

<output>
Create `.planning/phases/04-ssh-executor/04-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol | Kind | File |
|--------|------|------|
| `MOCK_SSH_FIXTURES` | exported constant (Record&lt;string, CommandResult&gt;) | `backend/src/ssh/mock.ts` |
| `DEFAULT_FALLBACK_RESULT` | exported constant (CommandResult) | `backend/src/ssh/mock.ts` |
| `MockSshExecutor` | class implementing SshExecutor | `backend/src/ssh/mock.ts` |
| `createMockSshExecutor()` | factory function | `backend/src/ssh/mock.ts` |
| `ssh-mock.test.ts` | TDD test file (10 tests) | `backend/src/tests/ssh-mock.test.ts` |
