---
phase: 06-run-api-approvals-sse
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - backend/src/app.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "POST /api/runs is reachable via the mounted app"
    - "GET /api/runs/:runId is reachable via the mounted app"
    - "POST /api/runs/:runId/next is reachable via the mounted app"
    - "POST /api/runs/:runId/abort is reachable via the mounted app"
    - "POST /api/runs/:runId/approvals/:approvalId/approve is reachable via the mounted app"
    - "POST /api/runs/:runId/approvals/:approvalId/reject is reachable via the mounted app"
    - "GET /api/runs/:runId/events returns SSE stream (200 text/event-stream)"
    - "All existing /health and /api/tickets routes continue to work"
  artifacts:
    - path: "backend/src/app.ts"
      provides: "Updated Hono app with runsRouter, approvalsRouter, eventsRouter mounted"
      contains: "app.route('/api/runs'"
  key_links:
    - from: "backend/src/app.ts"
      to: "backend/src/routes/runs.ts"
      via: "app.route('/api/runs', runsRouter)"
      pattern: "runsRouter"
    - from: "backend/src/app.ts"
      to: "backend/src/routes/approvals.ts"
      via: "app.route('/api/runs', approvalsRouter)"
      pattern: "approvalsRouter"
    - from: "backend/src/app.ts"
      to: "backend/src/routes/events.ts"
      via: "app.route('/api/runs', eventsRouter)"
      pattern: "eventsRouter"
---

<objective>
Mount the three new routers (runsRouter, approvalsRouter, eventsRouter) in backend/src/app.ts so all Phase 6 endpoints are reachable. This is a pure wiring task — no business logic.

Purpose: Routes defined in 06-01/02/03 are useless until app.ts mounts them. This is the [BLOCKING] integration step that makes the HTTP surface live.
Output: Updated backend/src/app.ts with three new app.route() calls; all routes verified reachable via vitest integration smoke.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@backend/src/app.ts
@backend/src/routes/runs.ts
@backend/src/routes/approvals.ts
@backend/src/routes/events.ts
@.planning/phases/06-run-api-approvals-sse/06-01-SUMMARY.md
@.planning/phases/06-run-api-approvals-sse/06-02-SUMMARY.md
@.planning/phases/06-run-api-approvals-sse/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount routers in app.ts and run full integration smoke test</name>
  <files>backend/src/app.ts</files>

  <read_first>
  - backend/src/app.ts — current mounts: app.route('/health', healthRouter); app.route('/api/tickets', ticketsRouter)
  - backend/src/routes/runs.ts — exports runsRouter (Hono)
  - backend/src/routes/approvals.ts — exports approvalsRouter (Hono)
  - backend/src/routes/events.ts — exports eventsRouter (Hono)
  - backend/src/tests/tickets.test.ts — import pattern: import { app } from '../app.js'; app.request(path)
  </read_first>

  <action>
  Read backend/src/app.ts, then add three import lines and three app.route() calls.

  Import runsRouter from './routes/runs.js', approvalsRouter from './routes/approvals.js', eventsRouter from './routes/events.ts'.

  Mount order (add after existing app.route('/api/tickets', ticketsRouter)):
    app.route('/api/runs', runsRouter);
    app.route('/api/runs', approvalsRouter);
    app.route('/api/runs', eventsRouter);

  All three routers mount at /api/runs. The runs router owns POST / and GET /:runId and POST /:runId/next and POST /:runId/abort. The approvals router owns POST /:runId/approvals/:approvalId/approve and POST /:runId/approvals/:approvalId/reject. The events router owns GET /:runId/events. Hono resolves them in order; path prefixes are distinct so there is no ambiguity.

  Do not remove or modify any existing imports or routes. Preserve the app.onError handler unchanged.

  After editing, run: cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx tsc --noEmit to confirm no type errors, then run pnpm test (or npx vitest run) to confirm existing tests still pass.

  For the Phase 6 integration smoke, the existing orchestrator.test.ts and tickets.test.ts cover the underlying layers. A simple HTTP smoke for the new routes is verified via the route-contract tests created in plans 06-01 and 06-02 which import { app } from '../app.js' and call app.request(). Those tests will now pass end-to-end because the routers are mounted.
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx tsc --noEmit 2>&1 | head -20 && npx vitest run 2>&1 | tail -30</automated>
  </verify>

  <done>
  backend/src/app.ts mounts runsRouter, approvalsRouter, eventsRouter at /api/runs; npx tsc --noEmit passes with no errors; npx vitest run is green (all tests pass including the new runs.test.ts and approvals.test.ts from plans 06-01 and 06-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app.ts route ordering | Three routers at /api/runs — Hono resolves by path match; no security boundary, just wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | Route mount order collision | accept | Hono path matching is deterministic; approve/reject paths (/approvals/:aid/approve) do not collide with /:runId/next or /:runId/events; verified by TypeScript compile + test pass |
| T-06-14 | Information Disclosure | app.onError leaking internals | mitigate | Existing onError in app.ts returns generic { error: 'internal server error' } — unchanged; never exposes stack traces or secret-bearing error text |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx tsc --noEmit && npx vitest run
</verification>

<success_criteria>
- backend/src/app.ts has three new app.route('/api/runs', ...) calls for runsRouter, approvalsRouter, eventsRouter
- npx tsc --noEmit passes with zero errors
- npx vitest run is green — all existing tests plus new route-contract tests pass
- GET /health and GET /api/tickets still return their expected responses (no regressions)
</success_criteria>

## Artifacts this plan produces

| Symbol | File | Description |
|--------|------|-------------|
| app (updated) | backend/src/app.ts | Hono app now mounts runsRouter + approvalsRouter + eventsRouter at /api/runs |

<output>
Create .planning/phases/06-run-api-approvals-sse/06-04-SUMMARY.md when done.
</output>
