---
phase: 06-run-api-approvals-sse
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/events/sse.ts
  - backend/src/routes/events.ts
autonomous: true
requirements:
  - API-03

must_haves:
  truths:
    - "Browser EventSource connecting to GET /api/runs/:runId/events receives prior audit events as backfill before live events"
    - "Live events emitted via runEventBus reach SSE subscribers with event: field matching the event type"
    - "Each SSE message is JSON { type, runId, ts, payload } mirroring audit row shape"
    - "A keepalive comment (:keepalive) is sent every ~15s to hold idle connections open"
    - "The runEventBus listener is removed when the client disconnects (onAbort cleanup)"
    - "Unknown runId returns 404 { error: 'run not found' } before opening the stream"
  artifacts:
    - path: "backend/src/events/sse.ts"
      provides: "createSseStream helper — subscribes to runEventBus and pipes events to Hono streamSSE"
      exports: ["createSseStream"]
    - path: "backend/src/routes/events.ts"
      provides: "eventsRouter Hono router — GET /api/runs/:runId/events"
      exports: ["eventsRouter"]
  key_links:
    - from: "backend/src/routes/events.ts"
      to: "backend/src/events/run-event-bus.ts"
      via: "runEventBus.on / runEventBus.off inside streamSSE callback"
      pattern: "runEventBus\\.on\\|runEventBus\\.off"
    - from: "backend/src/routes/events.ts"
      to: "backend/src/store/audit.ts"
      via: "getAuditEvents(runId) for backfill on connect"
      pattern: "getAuditEvents\\("
---

<objective>
Implement the SSE event stream route and its wiring helper. A browser connecting to GET /api/runs/:runId/events receives a backfill of prior audit events then live events as they happen, each formatted per PRD §9 shape. This gives the frontend real-time run progress without polling.

Purpose: SSE is glue/wiring — Hono streamSSE + EventEmitter subscription — no business logic to TDD. Standard execute plan.
Output: eventsRouter in backend/src/routes/events.ts and createSseStream helper in backend/src/events/sse.ts.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-run-api-approvals-sse/06-CONTEXT.md
@docs/PRD.md
@backend/src/events/run-event-bus.ts
@backend/src/store/audit.ts
@backend/src/store/schema.ts
@backend/src/store/runs.ts
@backend/src/routes/tickets.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SSE wiring helper (backend/src/events/sse.ts)</name>
  <files>backend/src/events/sse.ts</files>

  <read_first>
  - backend/src/events/run-event-bus.ts — RunEventBus.on/off/emit signatures; the single exported runEventBus singleton
  - backend/src/store/audit.ts — getAuditEvents(runId): AuditEvent[] — ordered by ts ASC; fields: id, run_id, type, actor, ts, payload_json
  - backend/src/store/schema.ts — AuditEvent type shape
  - Hono streamSSE API confirmed: streamSSE(c, async (stream) => { ... }); stream.writeSSE({ data, event, id }); stream.aborted; stream.onAbort(cb); stream.sleep(ms)
  </read_first>

  <action>
  Replace the stub in backend/src/events/sse.ts with a createSseStream export.

  createSseStream(c, runId): accepts a Hono Context and a runId string; returns the result of streamSSE(c, async (stream) => { ... }).

  Inside the streamSSE callback:

  1. Backfill: call getAuditEvents(runId). For each audit event in order, call stream.writeSSE with:
     - data: JSON.stringify({ type: event.type, runId: event.run_id, ts: event.ts, payload: JSON.parse(event.payload_json) })
     - event: event.type
     - id: event.id

  2. Register live listener: define listener(payload: unknown) that calls stream.writeSSE({ data: JSON.stringify({ type: eventType, runId, ts: new Date().toISOString(), payload }), event: eventType, id: ulid() }). The listener must be registered for every SSE event type that the orchestrator emits. Rather than subscribing per-event-type, subscribe to '*' wildcard — BUT runEventBus does not support wildcards. Instead, subscribe to each event type from the PRD §9 list: 'run.started', 'agent.thought_summary', 'command.proposed', 'command.blocked', 'approval.required', 'command.executing', 'command.completed', 'observation.added', 'fix.proposed', 'validation.completed', 'activity.drafted', 'activity.submitted', 'run.completed', 'run.failed'. For each eventType, register runEventBus.on(runId, eventType, listener) where listener closes over eventType.

  Note: Each listener write is async but the EventEmitter fires synchronously. Use void stream.writeSSE(...) inside the listener (fire-and-forget — the SSE stream buffers internally).

  3. Keepalive loop: use a setInterval (or stream.sleep loop). A setInterval approach is cleaner: every 15000ms write a raw SSE comment. Hono's writeSSE does not support raw comment lines directly — use a workaround: write an event with event: 'keepalive' and data: '' (empty). This triggers a named event the browser can ignore. Alternatively, keep a while (!stream.aborted) loop with stream.sleep(15000) and write the keepalive inside.

  Use the sleep loop approach: while (!stream.aborted) { await stream.sleep(15000); if (!stream.aborted) { await stream.writeSSE({ data: '', event: 'keepalive', id: '' }); } }

  4. Cleanup on disconnect: stream.onAbort(() => { for each eventType in the list: runEventBus.off(runId, eventType, listener) }). This must be registered before the sleep loop starts.

  Import: import { streamSSE } from 'hono/streaming'; import { ulid } from 'ulid'; import { runEventBus } from './run-event-bus.js'; import { getAuditEvents } from '../store/audit.js'; import type { Context } from 'hono';

  The SSE_EVENT_TYPES constant (string array of the 14 PRD event types) should be exported so events.ts and tests can import it without duplicating the list.

  No fenced code in action — implement using the identifiers and patterns above.
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx tsc --noEmit 2>&1 | grep -v '^$' | head -20</automated>
  </verify>

  <done>
  backend/src/events/sse.ts exports createSseStream and SSE_EVENT_TYPES; npx tsc --noEmit passes with no errors in sse.ts; all 14 event types from PRD §9 are in SSE_EVENT_TYPES.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Events route (backend/src/routes/events.ts)</name>
  <files>backend/src/routes/events.ts</files>

  <read_first>
  - backend/src/events/sse.ts — createSseStream signature (written in Task 1)
  - backend/src/store/runs.ts — getRunById(id): Run | undefined
  - backend/src/routes/tickets.ts — Hono router pattern: new Hono(); router.get('/', handler); c.req.param()
  </read_first>

  <action>
  Replace the stub in backend/src/routes/events.ts with a proper Hono router.

  Import: Hono from 'hono'; getRunById from '../store/runs.js'; createSseStream from '../events/sse.js'.

  Export eventsRouter = new Hono().

  eventsRouter.get('/:runId/events', async (c) => {
    const runId = c.req.param('runId');
    const run = getRunById(runId);
    if (!run) return c.json({ error: 'run not found' }, 404);
    return createSseStream(c, runId);
  });

  The 404 check before opening the SSE stream is important — streamSSE sets Content-Type: text/event-stream immediately; returning JSON 404 must happen before that.

  No other routes in this file. Keep it minimal.
  </action>

  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx tsc --noEmit 2>&1 | grep -v '^$' | head -20</automated>
  </verify>

  <done>
  backend/src/routes/events.ts exports eventsRouter; npx tsc --noEmit passes; GET /api/runs/unknown/events returns 404 { error: "run not found" } when tested against a running server (verified via curl or app.request in manual smoke test).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream payload → browser | Audit payloads are already redacted at write time (appendAuditEvent calls redactSecrets); backfill reads already-redacted rows; live events come from orchestrator which also redacts before emitting |
| runId URL param → runEventBus subscription | Unknown runId checked via getRunById before opening stream; listener leak prevented by onAbort cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | SSE backfill payload | mitigate | audit_events.payload_json is written via appendAuditEvent which calls redactSecrets before insertion; backfill reads already-redacted rows — no re-redaction needed |
| T-06-10 | Denial of Service | Listener leak on disconnect | mitigate | stream.onAbort unregisters all runEventBus.off calls for the 14 event types; setMaxListeners(50) already set on RunEventBus emitters |
| T-06-11 | Denial of Service | Unknown runId floods bus subscriptions | mitigate | getRunById check returns 404 before any listener is registered |
| T-06-12 | Spoofing | runId param cross-run data leak | accept | Single-team local tool, no auth (ARCHITECTURE.md §"No backend auth"); runId is opaque ulid; no cross-user scenario |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — uses existing hono, ulid, and internal modules |
</threat_model>

<verification>
cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx tsc --noEmit 2>&1 | head -20
</verification>

<success_criteria>
- backend/src/events/sse.ts: createSseStream and SSE_EVENT_TYPES exported; all 14 PRD §9 event types present
- backend/src/routes/events.ts: eventsRouter exported with GET /:runId/events handler
- Unknown runId → 404 before stream opens (no Content-Type: text/event-stream sent)
- TypeScript compiles clean (npx tsc --noEmit passes)
- Listeners registered in createSseStream are cleaned up in onAbort callback
</success_criteria>

## Artifacts this plan produces

| Symbol | File | Description |
|--------|------|-------------|
| createSseStream | backend/src/events/sse.ts | Core SSE helper — backfill + live subscription + keepalive + cleanup |
| SSE_EVENT_TYPES | backend/src/events/sse.ts | 14-element string[] of PRD §9 SSE event type names |
| eventsRouter | backend/src/routes/events.ts | Hono router — GET /:runId/events |

<output>
Create .planning/phases/06-run-api-approvals-sse/06-03-SUMMARY.md when done.
</output>
