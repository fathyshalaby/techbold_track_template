---
phase: 05-agent-loop-orchestrator
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/src/ai/model.ts
  - backend/src/ai/prompts.ts
autonomous: true
requirements:
  - DIAG-02

must_haves:
  truths:
    - "model.ts exports a single getModel() that returns the real openai provider when LLM_MODEL env is set, and a mock language model object when resolveClientMode('llm') === 'mock'"
    - "prompts.ts exports one system prompt string per agent role — problem_analyzer, customer_system_analyzer, problem_solver, validator"
    - "No agent system prompt contains hardcoded hostnames, ticket IDs, or symptom strings (SC2 / DIAG-02)"
    - "Every system prompt includes the shared safety rules: propose only, one command per turn, exhaust diagnosis before fix, never invent facts"
  artifacts:
    - path: backend/src/ai/model.ts
      provides: "getModel() factory — real openai or mock LanguageModelV1"
      exports:
        - getModel
    - path: backend/src/ai/prompts.ts
      provides: "System prompt strings per agent role"
      exports:
        - PROBLEM_ANALYZER_SYSTEM_PROMPT
        - CUSTOMER_SYSTEM_ANALYZER_SYSTEM_PROMPT
        - PROBLEM_SOLVER_SYSTEM_PROMPT
        - VALIDATOR_SYSTEM_PROMPT
  key_links:
    - from: backend/src/ai/model.ts
      to: backend/src/ai/agents/problem-analyzer.ts
      via: "getModel() passed to generateObject({ model })"
    - from: backend/src/ai/prompts.ts
      to: backend/src/ai/agents/problem-analyzer.ts
      via: "PROBLEM_ANALYZER_SYSTEM_PROMPT used as system message"
---

<objective>
Wire the AI SDK provider behind a single getModel() factory that switches between the real
@ai-sdk/openai provider and a deterministic mock, and define the four system prompt strings
that each agent will use verbatim.

Purpose: Agents import getModel() and their prompt constant — no provider config or prompt text
lives in agent files. The mock path ensures the full loop runs offline under MOCK_LLM=true.
Output: model.ts and prompts.ts — both are pure wiring/config with no business logic to TDD.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/PROJECT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/docs/ARCHITECTURE.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/phases/05-agent-loop-orchestrator/05-CONTEXT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/env.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/ai/model.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/ai/prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement model.ts — real openai provider + deterministic mock</name>
  <files>backend/src/ai/model.ts</files>
  <read_first>
    - backend/src/env.ts — getEnv() returns LLM_MODEL, OPENAI_API_KEY; resolveClientMode('llm') returns 'mock' | 'real'
    - backend/src/ai/model.ts — current stub (export {})
    - docs/ARCHITECTURE.md §1 — "Vercel AI SDK v5" reference (actual installed version is ai@4.3.x; use generateObject API accordingly)
    - backend/package.json — confirms ai@^4.3.16 and @ai-sdk/openai installed
  </read_first>
  <action>
    Replace the stub in backend/src/ai/model.ts.

    Import createOpenAI from '@ai-sdk/openai'. Import resolveClientMode, getEnv from '../env.js'.
    Import LanguageModelV1 from 'ai'.

    getModel(): LanguageModelV1 — checks resolveClientMode('llm'):
    - 'mock' → return a mock object that satisfies LanguageModelV1. The mock must be usable with
      generateObject; the simplest approach is an object with specificationVersion:'v1',
      provider:'mock', modelId:'mock', defaultObjectGenerationMode:'json', and a doGenerate
      method that returns a minimal valid response. The mock does NOT need to produce valid
      agent output — agents use their own scripted mock responses injected at the agent call
      site (see Plan 03). The model mock only needs to not throw when generateObject is called
      in unit tests that bypass the real agent functions.
    - 'real' → call createOpenAI({ apiKey: getEnv().OPENAI_API_KEY }) and return the model
      instance for getEnv().LLM_MODEL (e.g. 'gpt-4o').

    Export getModel as a named export. No default export.

    Do NOT wrap in a singleton cache — callers construct per-call to allow test isolation.
  </action>
  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx tsc --noEmit 2>&1 | grep -v '^$' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/ai/model.ts exports getModel
    - tsc --noEmit passes with no errors in model.ts
    - resolveClientMode('llm') === 'mock' path returns an object with modelId === 'mock'
    - resolveClientMode('llm') === 'real' path calls createOpenAI and returns a LanguageModelV1
    - No OPENAI_API_KEY value is hardcoded or logged
  </acceptance_criteria>
  <done>model.ts implemented; tsc clean; mock and real paths verified by inspection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement prompts.ts — four role system prompts, no hardcoded incident data</name>
  <files>backend/src/ai/prompts.ts</files>
  <read_first>
    - backend/src/ai/prompts.ts — current stub (export {})
    - docs/ARCHITECTURE.md §9 — role-specific prompt sketches and shared safety rules
    - docs/SAFETY_POLICY.md §1 — non-negotiable rules to bake into every prompt
    - .planning/phases/05-agent-loop-orchestrator/05-CONTEXT.md §Specific Ideas — validation honesty (never is-active), persistence graded
  </read_first>
  <behavior>
    Generalization test (DIAG-02 / SC2) — added to orchestrator.test.ts:
    - PROBLEM_ANALYZER_SYSTEM_PROMPT does not match /\b(status-api|vm-01|EADDRINUSE)\b/i
    - CUSTOMER_SYSTEM_ANALYZER_SYSTEM_PROMPT does not match /\b(status-api|vm-01|ticket[_-]?\d+)\b/i
    - PROBLEM_SOLVER_SYSTEM_PROMPT does not match /\b(status-api|vm-01|8080)\b/i
    - VALIDATOR_SYSTEM_PROMPT does not match /\b(is-active|vm-01|status-api)\b/i
    - Each prompt string length > 200 (non-trivial content)
    - VALIDATOR_SYSTEM_PROMPT matches /LIKELY_FIXED/ and /VERIFIED_FIXED/ (honesty distinction present)
  </behavior>
  <action>
    Replace the stub in backend/src/ai/prompts.ts.

    Export four const strings. Each prompt must:
    1. Open with the shared safety preamble from ARCHITECTURE §9: "You propose; you never execute.
       A human approves every command. Use only facts from the provided ticket and observations.
       Never invent results. One command per turn. State the expected signal. Exhaust diagnosis
       before proposing any fix. Fixes must be minimal and reversible."
    2. Include role-specific instructions derived from ARCHITECTURE §9 sketches.

    PROBLEM_ANALYZER_SYSTEM_PROMPT — scope to local Linux services (systemd, ports, disk,
    permissions, logs, cron, deps). Instruct: output DiagnosticProposal with ranked hypotheses
    listing evidence and confidence, then one diagnostic command. Prefer read-only commands.
    Mention specific read-only tools: systemctl status, journalctl, ss -tulpn, df -h, free -m,
    tail/grep of a specific log.

    CUSTOMER_SYSTEM_ANALYZER_SYSTEM_PROMPT — read-only probes to establish OS, running services,
    open ports, disk/memory state. Output a system context summary for use by the analyzer.

    PROBLEM_SOLVER_SYSTEM_PROMPT — output FixProposal: address root cause not symptom, minimal
    reversible command with rollback. Prefer reboot-persistent fixes (systemctl enable, fix
    persistent config). No broad destructive ops.

    VALIDATOR_SYSTEM_PROMPT — output ValidationResult. Proof MUST be a customer-benefit test
    (e.g. curl returns 200), never systemctl is-active alone. Single success → LIKELY_FIXED.
    Repeated success with persistence check (survives restart/reboot) → VERIFIED_FIXED.
    NOT_FIXED if the benefit test fails.

    No hostname, ticket ID, port number, service name, or symptom string from the practice VMs
    may appear in any prompt. Runtime context (ticket description, observations) is injected
    as user-turn content by the agent caller — not baked into the system prompt.
  </action>
  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend && npx vitest run src/tests/orchestrator.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/ai/prompts.ts exports PROBLEM_ANALYZER_SYSTEM_PROMPT, CUSTOMER_SYSTEM_ANALYZER_SYSTEM_PROMPT, PROBLEM_SOLVER_SYSTEM_PROMPT, VALIDATOR_SYSTEM_PROMPT
    - All four generalization grep-tests pass (no hardcoded hostnames/IDs/ports/service-names)
    - VALIDATOR_SYSTEM_PROMPT contains 'LIKELY_FIXED' and 'VERIFIED_FIXED' distinguishing the two states
    - VALIDATOR_SYSTEM_PROMPT does not contain 'is-active' as a standalone proof instruction
    - tsc --noEmit clean for prompts.ts
    - orchestrator.test.ts generalization block GREEN
  </acceptance_criteria>
  <done>prompts.ts implemented; generalization tests green; no incident-specific strings present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars → model config | OPENAI_API_KEY read via getEnv(); never logged or returned to client |
| system prompt → LLM | Prompt injection: command output (untrusted) is injected as user-turn content, never merged into the system prompt string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | model.ts / OPENAI_API_KEY | mitigate | Key read from getEnv() only; never in logs, never in responses; env.ts already masks values in error messages |
| T-05-04 | Tampering | prompts.ts — prompt injection | mitigate | Runtime context (ticket, observations) injected as user-turn content by agent caller; system prompt is a static module constant and cannot be modified by command output |
| T-05-05 | Repudiation | Hardcoded incident data in prompts | mitigate | Generalization test (SC2) asserts no fixture hostnames/IDs in prompt strings; CI catches regressions |
| T-05-SC | Tampering | npm installs | accept | No new packages; @ai-sdk/openai and ai already in package.json |
</threat_model>

<verification>
- npx vitest run exits 0 for all suites
- npx tsc --noEmit exits 0
- grep -rn 'status-api\|vm-01\|EADDRINUSE\|8080' backend/src/ai/prompts.ts returns no matches
</verification>

<success_criteria>
- getModel() returns a LanguageModelV1 for both mock and real modes
- Four prompt constants defined; none contain hardcoded incident data
- Generalization test added to orchestrator.test.ts and green
- tsc --noEmit clean
</success_criteria>

<output>
Create .planning/phases/05-agent-loop-orchestrator/05-02-SUMMARY.md when done
</output>
