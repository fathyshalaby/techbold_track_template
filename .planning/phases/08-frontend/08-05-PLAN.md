---
phase: 08-frontend
plan: 05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - frontend/src/components/ActivityView.tsx
  - frontend/src/index.css
autonomous: true
requirements:
  - UX-06

must_haves:
  truths:
    - "Activity editor shows five pre-filled textareas: summary, rootCause, actionsTaken, commandsSummary, validationResult"
    - "Each field is editable; content initialized from the activityDraft fetched via GET /api/runs/:id"
    - "Submit posts only the non-empty overrides to POST /api/runs/:id/activity/submit"
    - "Submitting shows a spinner; success shows a confirmation message; error shows an inline banner"
    - "A Back button navigates back to the run view without submitting"
  artifacts:
    - path: "frontend/src/components/ActivityView.tsx"
      provides: "5-field activity editor with submit/back controls"
      exports: ["ActivityView"]
  key_links:
    - from: "frontend/src/components/ActivityView.tsx"
      to: "frontend/src/api.ts"
      via: "submitActivity(runId, overrides)"
      pattern: "submitActivity"
---

<objective>
Replace the ActivityView stub with the full activity editor: five editable
textareas pre-filled from the activityDraft, a Submit button that posts
overrides to /activity/submit, and a Back button. Covers UX-06.

Purpose: The technician's last step — review the AI-drafted activity report,
edit any field, then submit to Phoenix ERP.
Output: ActivityView.tsx as a standalone component replacing the stub; minor
CSS additions to index.css.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/phases/08-frontend/08-CONTEXT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/src/types.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/src/api.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/routes/activity.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActivityView component with five-field editor</name>
  <files>
    frontend/src/components/ActivityView.tsx
    frontend/src/index.css
  </files>
  <read_first>
    - frontend/src/components/ActivityView.tsx — current stub; props interface must stay identical
    - frontend/src/types.ts — ActivityDraft type: summary, root_cause, actions_taken, commands_summary, validation_result (snake_case from API)
    - frontend/src/api.ts — submitActivity(runId, overrides: Partial&lt;ActivitySubmitBody&gt;) signature; ActivitySubmitBody uses camelCase (summary, rootCause, actionsTaken, commandsSummary, validationResult)
    - backend/src/routes/activity.ts — SubmitBodySchema fields for submit body shape reference
    - frontend/src/index.css — existing styles to extend
  </read_first>
  <behavior>
    - Fields initialize from activityDraft prop (snake_case → camelCase mapping): summary→summary, root_cause→rootCause, actions_taken→actionsTaken, commands_summary→commandsSummary, validation_result→validationResult
    - When activityDraft is null all fields initialize to empty string
    - Submit sends only fields that differ from the original draft (or all fields if draft was null)
    - Submitting disables Submit button and shows "Submitting…" text
    - On success: shows "Activity submitted successfully." confirmation; hides form controls
    - On error: shows error message in div class "activity-error"
    - Back button always visible; calls onBack() immediately (no async)
  </behavior>
  <action>
    Replace frontend/src/components/ActivityView.tsx.
    Props interface stays identical to the stub (runId, activityDraft, onBack) — DO NOT change it.

    Internal state:
      summary: string
      rootCause: string
      actionsTaken: string
      commandsSummary: string
      validationResult: string
      submitting: boolean — default false
      submitted: boolean — default false
      error: string | null — default null

    Initialize state fields from activityDraft in a useMemo or directly in useState
    initializers (activityDraft?.summary ?? '', etc.).

    Layout (class "activity-view"):
      h2 "Activity Report"
      p class "activity-subtitle" — "Review and edit the AI-generated report before submitting to Phoenix."

      For each of the five fields render a label + textarea pair:
        summary: label "Summary", textarea class "activity-field" id "field-summary"
        rootCause: label "Root Cause", textarea class "activity-field" id "field-root-cause"
        actionsTaken: label "Actions Taken", textarea class "activity-field" id "field-actions-taken"
        commandsSummary: label "Commands Summary", textarea class "activity-field" id "field-commands-summary"
        validationResult: label "Validation Result", textarea class "activity-field" id "field-validation-result"

      Each textarea: rows=5, value bound to state, onChange updates state.
      All textareas and controls disabled when submitting=true or submitted=true.

      Controls row (class "activity-controls"):
        Back button (class "btn btn-back") — always enabled, calls onBack()
        Submit button (class "btn btn-submit") — shows "Submitting…" when submitting; disabled when submitting || submitted

      If submitted: show div class "activity-success" with text "Activity submitted successfully."
      If error: show div class "activity-error" with the error message string (text content only)

    handleSubmit():
      Sets submitting true, clears error.
      Builds overrides object: include only fields whose current state value is non-empty.
      Calls submitActivity(runId, overrides).
      On success: sets submitted true.
      On error: sets error to the error message.
      Always sets submitting false (unless submitted, where the form is frozen anyway).

    Extend frontend/src/index.css with styles for:
      activity-view, activity-subtitle, activity-field (width: 100%, font-family: inherit,
      resize: vertical, padding: 8px), activity-controls, activity-success
      (color: #2e7d32, border: 1px solid #a5d6a7, padding: 8px 12px, border-radius: 4px),
      activity-error (color: #c62828, border: 1px solid #ef9a9a, padding: 8px 12px,
      border-radius: 4px), btn-back, btn-submit.

    Final index.css pass: ensure shared button base class "btn" has display: inline-flex,
    padding: 8px 16px, border-radius: 4px, cursor: pointer, border: none, font-size: 14px.
    Verify no class names referenced in plans 08-03 and 08-04 are missing from index.css.
  </action>
  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    ActivityView.tsx compiles with no tsc errors.
    Prop interface matches the stub defined in plan 08-03 exactly.
    Five labeled textareas render and are bound to state.
    Submit calls submitActivity with the overrides object (camelCase keys).
    Success message shown after submit; error shown on failure.
    Back button calls onBack() without async side effects.
    index.css contains activity-view, activity-field, activity-success, activity-error,
    and the shared btn base styles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activityDraft prop → textarea | Draft content from backend rendered as textarea value |
| Textarea value → /activity/submit | User-edited text sent back to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | XSS | activityDraft field display | accept | Fields rendered as textarea value attributes (React-controlled); no innerHTML; textareas escape content by design |
| T-08-16 | Information Disclosure | activity draft containing secrets | accept | Backend already redacts secrets before saving draft (redactSecrets in activity.ts); frontend renders what it receives; no client-side de-redaction |
| T-08-17 | Tampering | submit overrides bypass | accept | Backend applies redactSecrets again on all submitted field values at submit time; no security bypass path on client |
| T-08-SC | Tampering | npm installs | accept | No new npm packages added this plan |
</threat_model>

<verification>
- tsc --noEmit clean after task
- Five textareas present and bound to state variables
- submitActivity called with camelCase override keys
- submitted=true freezes form and shows success message
- error state shown on failure
- Back button works without async
</verification>

<success_criteria>
- frontend/src/components/ActivityView.tsx replaces stub with full five-field editor
- tsc --noEmit passes across entire frontend
- index.css is complete with all class names used across plans 08-03 through 08-05
</success_criteria>

<output>
Create `.planning/phases/08-frontend/08-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| ActivityView | frontend/src/components/ActivityView.tsx | component (replaces stub) |
