---
phase: 08-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types.ts
  - frontend/src/api.ts
  - frontend/src/utils/mappers.ts
  - frontend/src/utils/mappers.test.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "All shared TypeScript types are defined in one place and imported by hooks and components"
    - "All fetch calls to the backend go through api.ts wrappers, never raw fetch inline"
    - "riskBadge returns the correct color class for all four RiskLevel values"
    - "sseEventLabel returns a non-empty icon and label string for every SSE_EVENT_TYPES entry"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "Ticket, CustomerSystem, Run, CommandApproval, ActivityDraft, SseEvent TypeScript types"
      exports: ["Ticket", "CustomerSystem", "Run", "CommandApproval", "ActivityDraft", "SseEvent", "RiskLevel"]
    - path: "frontend/src/api.ts"
      provides: "Typed fetch wrappers for all backend endpoints"
      exports: ["listTickets", "getTicket", "getCustomerSystem", "createRun", "getRun", "advanceRun", "abortRun", "approveCommand", "rejectCommand", "draftActivity", "submitActivity"]
    - path: "frontend/src/utils/mappers.ts"
      provides: "riskBadge and sseEventLabel pure mappers"
      exports: ["riskBadge", "sseEventLabel"]
    - path: "frontend/src/utils/mappers.test.ts"
      provides: "Vitest tests for both mappers"
  key_links:
    - from: "frontend/src/api.ts"
      to: "import.meta.env.VITE_API_BASE"
      via: "const BASE = import.meta.env.VITE_API_BASE ?? 'http://localhost:8000'"
      pattern: "VITE_API_BASE"
    - from: "frontend/src/types.ts"
      to: "backend/src/store/schema.ts"
      via: "mirrors CommandApproval shape (proposed_command, purpose, expected_signal, risk_level, safety_notes)"
      pattern: "CommandApproval"
---

<objective>
Define all shared TypeScript types, typed API fetch wrappers, and the two pure
mapper functions consumed by every component in this phase.

Purpose: Establishing the contract layer first means every subsequent plan
builds against known types rather than using `any` or re-deriving shapes.
Output: types.ts, api.ts, mappers.ts, and a passing test file for the mappers.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/PROJECT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/ROADMAP.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/STATE.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/phases/08-frontend/08-CONTEXT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/store/schema.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/events/sse.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/src/App.tsx
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared TypeScript types (types.ts)</name>
  <files>frontend/src/types.ts</files>
  <read_first>
    - backend/src/store/schema.ts — CommandApproval, Run, AuditEvent, ActivityDraft shapes
    - backend/src/events/sse.ts — SSE_EVENT_TYPES constant (the full 14-entry tuple)
    - frontend/src/App.tsx — existing file to understand starting point
  </read_first>
  <action>
    Create frontend/src/types.ts with the following TypeScript interfaces/types
    (all interfaces, PascalCase, no `I` prefix, strict — no `any`):

    RiskLevel: string union of the four backend values:
    "SAFE_READ_ONLY" | "LOW_RISK_CHANGE" | "MEDIUM_RISK_CHANGE" | "HIGH_RISK_BLOCKED"

    SseEventType: string union derived from all 14 entries in SSE_EVENT_TYPES in
    backend/src/events/sse.ts (run.started, agent.thought_summary, command.proposed,
    command.blocked, approval.required, command.executing, command.completed,
    observation.added, fix.proposed, validation.completed, activity.drafted,
    activity.submitted, run.completed, run.failed).

    SseEvent: { type: SseEventType; runId: string; ts: string; payload: unknown }

    Ticket: { id: number; title: string; priority: string; status: string;
              customer_name: string }

    CustomerSystem: { ip: string; port: number; username: string; os: string }

    CommandApproval: mirrors backend CommandApproval from schema.ts —
    id, run_id, proposed_command, edited_command (null|string),
    final_command (null|string), purpose, expected_signal,
    risk_level (RiskLevel), safety_notes, status
    ("PENDING"|"APPROVED"|"REJECTED"|"EXECUTED"|"BLOCKED"),
    technician_reason (null|string), created_at, decided_at (null|string),
    executed_at (null|string)

    ActivityDraft: { summary: string; root_cause: string; actions_taken: string;
                     commands_summary: string; validation_result: string }

    Run: {
      runId: string;
      status: string;
      phase: string;
      timeline: AuditEvent[];
      pendingApproval: CommandApproval | null;
      activityDraft: ActivityDraft | null;
    }

    AuditEvent: { id: string; run_id: string; type: string; actor: string;
                  ts: string; payload_json: string }

    CreateRunResult: { runId: string; status: string; ticket: Ticket;
                       customerSystem: CustomerSystem }

    Export all types. No default export.
  </action>
  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>frontend/src/types.ts exists, tsc --noEmit reports no errors in that file, all named types exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API fetch wrappers (api.ts) + pure mappers with tests</name>
  <files>
    frontend/src/api.ts
    frontend/src/utils/mappers.ts
    frontend/src/utils/mappers.test.ts
  </files>
  <read_first>
    - frontend/src/types.ts — types just created
    - backend/src/routes/tickets.ts — query params for listTickets
    - backend/src/routes/runs.ts — POST body shapes, response shapes
    - backend/src/routes/approvals.ts — approve/reject body schemas
    - backend/src/routes/activity.ts — SubmitBodySchema (5 optional string fields)
    - backend/src/events/sse.ts — SSE_EVENT_TYPES for SseEventType union
  </read_first>
  <behavior>
    mappers.test.ts — riskBadge:
    - riskBadge("SAFE_READ_ONLY") returns { label: "SAFE READ ONLY", colorClass: "badge--safe" }
    - riskBadge("LOW_RISK_CHANGE") returns { label: "LOW RISK", colorClass: "badge--low" }
    - riskBadge("MEDIUM_RISK_CHANGE") returns { label: "MEDIUM RISK", colorClass: "badge--medium" }
    - riskBadge("HIGH_RISK_BLOCKED") returns { label: "HIGH RISK — BLOCKED", colorClass: "badge--high" }

    mappers.test.ts — sseEventLabel:
    - sseEventLabel("run.started") returns { icon: "▶", label: "Run started" }
    - sseEventLabel("approval.required") returns { icon: "⏳", label: "Approval required" }
    - sseEventLabel("command.proposed") returns { icon: "💡", label: "Command proposed" }
    - sseEventLabel("command.blocked") returns { icon: "🚫", label: "Command blocked" }
    - sseEventLabel("command.executing") returns { icon: "⚙", label: "Executing command" }
    - sseEventLabel("command.completed") returns { icon: "✓", label: "Command completed" }
    - sseEventLabel("observation.added") returns { icon: "🔍", label: "Observation" }
    - sseEventLabel("fix.proposed") returns { icon: "🔧", label: "Fix proposed" }
    - sseEventLabel("validation.completed") returns { icon: "✅", label: "Validation complete" }
    - sseEventLabel("activity.drafted") returns { icon: "📝", label: "Activity drafted" }
    - sseEventLabel("activity.submitted") returns { icon: "📤", label: "Activity submitted" }
    - sseEventLabel("run.completed") returns { icon: "🏁", label: "Run completed" }
    - sseEventLabel("run.failed") returns { icon: "❌", label: "Run failed" }
    - sseEventLabel("agent.thought_summary") returns { icon: "🧠", label: "Agent thinking" }
    - unknown type returns { icon: "•", label: the input string }
  </behavior>
  <action>
    Create frontend/src/utils/mappers.ts first (RED phase):

    Export riskBadge(level: RiskLevel): { label: string; colorClass: string }
    Map all four RiskLevel values to their label and colorClass strings per
    the behavior block. colorClass values: "badge--safe", "badge--low",
    "badge--medium", "badge--high". Return { label: level, colorClass: "badge--safe" }
    as the fallback (TypeScript exhaustive — the union covers all cases).

    Export sseEventLabel(type: string): { icon: string; label: string }
    Map all 14 SseEventType values to their icon/label pair per the behavior
    block. Default case: { icon: "•", label: type }.

    Write mappers.test.ts with Vitest (import { describe, it, expect } from "vitest").
    Assert every mapping listed in the behavior block. Run RED first (empty stubs
    that throw), then GREEN (full implementation). Tests must pass.

    Create frontend/src/api.ts:

    const BASE = import.meta.env.VITE_API_BASE ?? "http://localhost:8000";

    Helper: async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>
    — builds URL from BASE + path, calls fetch, throws Error with the backend
    "error" field text (or HTTP status text) on non-2xx, returns parsed JSON body.
    Surface the backend error message exactly (used by error banners in components).

    Export these functions (all async, typed with types from types.ts):
    - listTickets(params?: { status?: string; priority?: string; sort?: string }): Promise<Ticket[]>
      GET /api/tickets with query string. On 502 or empty array, return [] (per ERP-05).
    - getTicket(id: number): Promise<Ticket>
      GET /api/tickets/:id
    - getCustomerSystem(id: number): Promise<CustomerSystem>
      GET /api/tickets/:id/customer-system
    - createRun(ticketId: number): Promise<CreateRunResult>
      POST /api/runs { ticketId }
    - getRun(runId: string): Promise<Run>
      GET /api/runs/:runId
    - advanceRun(runId: string): Promise<{ status: string; phase: string; pendingApproval: CommandApproval | null }>
      POST /api/runs/:runId/next
    - abortRun(runId: string): Promise<{ status: string; phase: string }>
      POST /api/runs/:runId/abort
    - approveCommand(runId: string, approvalId: string, editedCommand?: string): Promise<unknown>
      POST /api/runs/:runId/approvals/:approvalId/approve { editedCommand? }
      Must NOT swallow 422 — let apiFetch throw so the caller can surface the
      "command blocked by safety policy" message.
    - rejectCommand(runId: string, approvalId: string, reason: string): Promise<unknown>
      POST /api/runs/:runId/approvals/:approvalId/reject { reason }
    - draftActivity(runId: string): Promise<ActivityDraft>
      POST /api/runs/:runId/activity/draft
    - submitActivity(runId: string, overrides: Partial<ActivityDraft>): Promise<unknown>
      POST /api/runs/:runId/activity/submit with camelCase keys matching SubmitBodySchema
      (summary, rootCause, actionsTaken, commandsSummary, validationResult)

    No default export from api.ts or mappers.ts.
  </action>
  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend && npx vitest run src/utils/mappers.test.ts 2>&1</automated>
  </verify>
  <done>
    mappers.test.ts passes all assertions (all 19 cases green).
    api.ts exports all 11 functions with correct TypeScript signatures.
    tsc --noEmit reports no errors in api.ts or mappers.ts.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → backend API | All data originates from the backend; frontend renders it only |
| api.ts error handling | Backend 4xx/5xx messages may contain user-facing text; surfaced as-is in error banners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | api.ts — VITE_API_BASE | accept | Base URL is non-secret; no credentials stored in frontend code or environment |
| T-08-02 | Information Disclosure | api.ts — error messages from backend | accept | Backend already redacts secrets before sending; frontend surfaces the message string verbatim as text (React escapes) |
| T-08-03 | Tampering | approveCommand — edited command payload | mitigate | approveCommand sends editedCommand to backend which re-runs the safety gate; frontend does NOT pre-validate or bypass — 422 must propagate to caller |
| T-08-SC | Tampering | npm installs (none in this plan) | accept | No new packages installed in this plan; existing package.json has no new deps |
</threat_model>

<verification>
- `npx tsc --noEmit` in frontend/ passes with no errors
- `npx vitest run src/utils/mappers.test.ts` — all 19 mapper assertions green
- All 11 api.ts functions are exported and have typed return signatures
- No `any` in types.ts or api.ts
- approveCommand does not catch 422 — it propagates the thrown error
</verification>

<success_criteria>
- types.ts exports all 8 named types (RiskLevel, SseEventType, SseEvent, Ticket, CustomerSystem, CommandApproval, ActivityDraft, Run, AuditEvent, CreateRunResult)
- api.ts exports 11 typed fetch wrappers; BASE reads VITE_API_BASE with fallback
- mappers.ts exports riskBadge and sseEventLabel; both handle all known values + default case
- mappers.test.ts is green (19 assertions)
- tsc --noEmit clean
</success_criteria>

<output>
Create `.planning/phases/08-frontend/08-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| RiskLevel | frontend/src/types.ts | type |
| SseEventType | frontend/src/types.ts | type |
| SseEvent | frontend/src/types.ts | interface |
| Ticket | frontend/src/types.ts | interface |
| CustomerSystem | frontend/src/types.ts | interface |
| CommandApproval | frontend/src/types.ts | interface |
| ActivityDraft | frontend/src/types.ts | interface |
| Run | frontend/src/types.ts | interface |
| AuditEvent | frontend/src/types.ts | interface |
| CreateRunResult | frontend/src/types.ts | interface |
| listTickets | frontend/src/api.ts | function |
| getTicket | frontend/src/api.ts | function |
| getCustomerSystem | frontend/src/api.ts | function |
| createRun | frontend/src/api.ts | function |
| getRun | frontend/src/api.ts | function |
| advanceRun | frontend/src/api.ts | function |
| abortRun | frontend/src/api.ts | function |
| approveCommand | frontend/src/api.ts | function |
| rejectCommand | frontend/src/api.ts | function |
| draftActivity | frontend/src/api.ts | function |
| submitActivity | frontend/src/api.ts | function |
| riskBadge | frontend/src/utils/mappers.ts | function |
| sseEventLabel | frontend/src/utils/mappers.ts | function |
</output>
