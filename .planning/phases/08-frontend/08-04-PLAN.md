---
phase: 08-frontend
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - frontend/src/components/RunView.tsx
  - frontend/src/components/ApprovalCard.tsx
  - frontend/src/index.css
autonomous: true
requirements:
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-07

must_haves:
  truths:
    - "Run page header shows customerSystem fields: ip, port, username, os (per UX-02)"
    - "Run page shows a live vertical timeline that auto-scrolls to the newest event"
    - "Approval card is pinned at top of run page when pendingApproval is non-null; hidden otherwise"
    - "Approval card shows proposed_command, purpose, expected_signal, risk badge, and safety_notes"
    - "Risk badge is color-coded: green SAFE_READ_ONLY, yellow LOW_RISK_CHANGE, orange MEDIUM_RISK_CHANGE, red HIGH_RISK_BLOCKED"
    - "Technician can Approve, Edit-then-approve (editable textarea), or Reject-with-reason (required text)"
    - "Approve/reject calls the correct backend endpoint; 422 blocked response is surfaced as an inline error"
    - "Abort button is always visible on the run page and calls POST /api/runs/:id/abort"
    - "An Advance/Continue button calls POST /api/runs/:id/next when the run is not waiting for approval and not terminal"
    - "Each timeline event shows a timestamped label; icon or prefix differs by SSE event type"
  artifacts:
    - path: "frontend/src/components/RunView.tsx"
      provides: "Run page: customer system header, timeline, approval card slot, abort/advance controls"
      exports: ["RunView"]
    - path: "frontend/src/components/ApprovalCard.tsx"
      provides: "Sticky approval UI: command display, edit textarea, approve/reject controls, risk badge"
      exports: ["ApprovalCard"]
  key_links:
    - from: "frontend/src/components/RunView.tsx"
      to: "frontend/src/hooks/useRunEvents.ts"
      via: "useRunEvents(runId) → { events, connected }"
      pattern: "useRunEvents"
    - from: "frontend/src/components/RunView.tsx"
      to: "frontend/src/hooks/useRun.ts"
      via: "useRun(runId) → { run, refresh } for pendingApproval + phase"
      pattern: "useRun"
    - from: "frontend/src/components/ApprovalCard.tsx"
      to: "frontend/src/api.ts"
      via: "approveCommand / rejectCommand"
      pattern: "approveCommand|rejectCommand"
    - from: "frontend/src/components/RunView.tsx"
      to: "frontend/src/api.ts"
      via: "advanceRun / abortRun"
      pattern: "advanceRun|abortRun"
---

<objective>
Replace the RunView stub with the full run page: a live SSE-driven timeline,
the pinned approval card (per D from 08-CONTEXT), customer system info header
(UX-02), and the abort/advance controls. This is the highest-stakes UI — it
delivers UX-02, UX-03, UX-04, UX-05, and UX-07.

Purpose: The technician drives the entire diagnostic loop from this view.
Output: RunView.tsx + ApprovalCard.tsx; both are functional React components
that consume the hooks and API client from plans 08-01 and 08-02.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/phases/08-frontend/08-CONTEXT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/src/types.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/src/api.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/src/hooks/useRunEvents.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/src/hooks/useRun.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/events/sse.ts
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/src/store/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ApprovalCard component</name>
  <files>
    frontend/src/components/ApprovalCard.tsx
  </files>
  <read_first>
    - frontend/src/types.ts — CommandApproval shape (proposed_command, purpose, expected_signal, risk_level, safety_notes, id)
    - frontend/src/api.ts — approveCommand(runId, approvalId, editedCommand?) and rejectCommand(runId, approvalId, reason) signatures; apiFetch throws with the backend "error" field verbatim so catch blocks use err.message directly
    - frontend/src/utils/mappers.ts — riskBadge(level: RiskLevel): { label: string; colorClass: string }; colorClass values are "badge--safe", "badge--low", "badge--medium", "badge--high" (double-dash, not "risk-safe" etc.)
    - backend/src/store/schema.ts — RiskLevel enum values: SAFE_READ_ONLY, LOW_RISK_CHANGE, MEDIUM_RISK_CHANGE, HIGH_RISK_BLOCKED
  </read_first>
  <behavior>
    - riskBadge('SAFE_READ_ONLY') → { label: 'SAFE READ ONLY', colorClass: 'badge--safe' }
    - riskBadge('LOW_RISK_CHANGE') → { label: 'LOW RISK', colorClass: 'badge--low' }
    - riskBadge('MEDIUM_RISK_CHANGE') → { label: 'MEDIUM RISK', colorClass: 'badge--medium' }
    - riskBadge('HIGH_RISK_BLOCKED') → { label: 'HIGH RISK — BLOCKED', colorClass: 'badge--high' }
    - Approve button calls approveCommand(runId, approvalId) with no editedCommand when not in edit mode
    - Edit-then-approve: textarea pre-filled with proposed_command; submit calls approveCommand with editedCommand
    - Reject: reason textarea required (min 1 char); submit calls rejectCommand(runId, approvalId, reason)
    - 422 response from approve surfaces "Command blocked by safety policy — edit or reject." inline (class "approval-error")
    - Other errors: show err.message directly (backend sends human-readable text; no re-mapping needed)
  </behavior>
  <action>
    Create frontend/src/components/ApprovalCard.tsx:

    Props:
      approval: CommandApproval
      runId: string
      onDecided: () => void  — called after a successful approve or reject so RunView can refresh

    Internal state:
      mode: 'default' | 'edit' | 'reject' — default 'default'
      editedCommand: string — initialized to approval.proposed_command when mode switches to 'edit'
      rejectReason: string — empty string
      submitting: boolean
      error: string | null

    Import riskBadge from frontend/src/utils/mappers.ts (NOT from a riskBadge.ts file — that
    file does not exist; the function lives in mappers.ts).

    Layout (class "approval-card"):
      Section header: "Awaiting your decision" (class "approval-header")
      Command block (class "approval-command"): monospace pre element showing approval.proposed_command
      Meta row (class "approval-meta"):
        - Purpose: {approval.purpose}
        - Expected signal: {approval.expected_signal}
        - Risk badge: span with classes "badge risk-badge {riskBadge(approval.risk_level).colorClass}"
          and text riskBadge(approval.risk_level).label
        - Safety notes: {approval.safety_notes} (only if non-empty string)

    Control section differs by mode:
      mode='default':
        - Button "Approve" (class "btn btn-approve") — calls handleApprove() without edited command
        - Button "Edit & Approve" (class "btn btn-edit") — sets mode to 'edit', copies proposed_command to editedCommand
        - Button "Reject" (class "btn btn-reject") — sets mode to 'reject'

      mode='edit':
        - Textarea (class "edit-command-textarea") bound to editedCommand; rows=4
        - Button "Submit edited command" — calls handleApprove(editedCommand)
        - Button "Cancel" — sets mode back to 'default'

      mode='reject':
        - Textarea (class "reject-reason-textarea") bound to rejectReason; placeholder "Reason (required)"
        - Button "Confirm reject" — disabled if rejectReason.trim() is empty; calls handleReject()
        - Button "Cancel" — sets mode back to 'default'

    handleApprove(cmd?: string):
      Sets submitting true, clears error.
      Calls approveCommand(runId, approval.id, cmd).
      On 422: sets error to "Command blocked by safety policy — edit or reject."
      On other error: sets error to err.message (backend error field is already human-readable).
      On success: calls onDecided().
      Always sets submitting false.

    handleReject():
      Sets submitting true, clears error.
      Calls rejectCommand(runId, approval.id, rejectReason).
      On error: sets error to err.message.
      On success: calls onDecided().
      Always sets submitting false.

    Distinguish 422 in handleApprove: catch the error and check if err.message includes
    "blocked" or "safety" (backend sends "command blocked by safety policy"); fall through
    to the generic message otherwise. Alternatively, since approveCommand re-throws on
    non-2xx, check err.message directly — the backend 422 body is
    { error: "command blocked by safety policy" } so err.message will be that string.

    Buttons disabled when submitting=true.
    Error shown in div class "approval-error" below controls (text content only, no innerHTML).
    All command/output text rendered in pre or textarea elements (never dangerouslySetInnerHTML).
  </action>
  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    ApprovalCard.tsx compiles cleanly.
    Three modes render correct controls.
    approveCommand called with editedCommand only in edit mode.
    rejectCommand called with reason.
    422 response shows approval-error message.
    Risk badge uses riskBadge() from mappers.ts — colorClass values are badge--safe/low/medium/high.
    Import path is frontend/src/utils/mappers.ts, not a non-existent riskBadge.ts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunView with live timeline, customer system header, approval card slot, and controls</name>
  <files>
    frontend/src/components/RunView.tsx
    frontend/src/index.css
  </files>
  <read_first>
    - frontend/src/components/RunView.tsx — current stub to replace; props interface must match exactly: { runId, ticketTitle, customerSystem, onActivityReady }
    - frontend/src/hooks/useRunEvents.ts — return shape is { events: SseEvent[]; connected: boolean } ONLY; it does NOT return pendingApproval or phase
    - frontend/src/hooks/useRun.ts — return shape: { run: Run | null; loading: boolean; error: string | null; refresh: () => void }; Run has pendingApproval: CommandApproval | null and phase: string
    - frontend/src/api.ts — advanceRun(runId), abortRun(runId) signatures; errors throw with err.message = backend error field verbatim
    - frontend/src/utils/mappers.ts — sseEventLabel(type: string): { icon: string; label: string }
    - frontend/src/types.ts — SseEvent, CustomerSystem types
    - frontend/src/index.css — existing styles to extend
  </read_first>
  <behavior>
    - pendingApproval and phase come from useRun(runId).run, NOT from useRunEvents
    - When useRun returns run.pendingApproval !== null, ApprovalCard is rendered pinned at top
    - After ApprovalCard calls onDecided(), RunView calls refresh() to re-fetch run state
    - On relevant SSE events (approval.required, command.executing, command.completed,
      run.completed, run.failed), RunView also calls refresh() to keep run state current
    - When pendingApproval is null and phase is not terminal, Advance button is shown
    - Terminal phases: COMPLETED, FAILED, ABORTED — Advance button hidden
    - When phase === WAITING_FOR_ACTIVITY_REVIEW: render "Review Activity Report" button; clicking calls onActivityReady()
    - Abort button always visible and calls abortRun(runId)
    - Timeline auto-scrolls to bottom when new events arrive (useEffect on events.length)
    - Each timeline row: timestamp (locale time), icon from sseEventLabel, label text
    - keepalive events are NOT rendered in the timeline (useRunEvents already has no listener for them)
    - Customer system header shows: ip, port, username, os from the customerSystem prop (render even if customerSystem is null with a fallback "–" per field)
    - On error: show err.message directly (no re-mapping)
  </behavior>
  <action>
    Replace frontend/src/components/RunView.tsx with the full implementation.
    Props interface must match the stub defined in plan 08-03 exactly:
      { runId: string; ticketTitle: string; customerSystem: CustomerSystem | null; onActivityReady: () => void }

    Hooks used:
      const { events, connected } = useRunEvents(runId);
      const { run, refresh } = useRun(runId);
      const pendingApproval = run?.pendingApproval ?? null;
      const phase = run?.phase ?? '';

    Additional internal state:
      aborting: boolean — default false
      advancing: boolean — default false
      actionError: string | null — default null

    useEffect on events: when a new event arrives whose type is one of
    ['approval.required', 'command.executing', 'command.completed',
    'run.completed', 'run.failed', 'validation.completed'],
    call refresh() to re-sync pendingApproval and phase from the server.
    Dependency array: [events.length].

    Layout (class "run-view"):
      Header row (class "run-header"):
        - h2 showing ticketTitle
        - Customer system info strip (class "customer-system-info"):
          four labelled spans — "IP: {ip}", "Port: {port}", "User: {username}", "OS: {os}"
          where each value falls back to "–" if customerSystem is null.
        - phase label (class "phase-label") showing phase
        - Connection indicator: small span showing connected ? "● live" : "○ connecting"
          (class "connection-indicator")
        - Abort button (class "btn btn-abort") always visible; disabled when aborting

      Approval card slot: if pendingApproval is non-null, render ApprovalCard with
        approval={pendingApproval}, runId={runId},
        onDecided={refresh}
        wrapped in a div class "approval-slot".

      Timeline section (class "timeline"):
        - Each SseEvent rendered as div class
          "timeline-event timeline-event-{event.type.replace('.', '-')}":
          span class "event-time" — new Date(event.ts).toLocaleTimeString()
          span class "event-icon" — sseEventLabel(event.type).icon
          span class "event-label" — sseEventLabel(event.type).label
          If event.payload is an object and contains a "command" string field, render
          it in a nested pre class "event-command" (text content only, never innerHTML).
          If event.payload is an object and contains a "summary" string field, render
          it as a p class "event-summary".
        - A ref (timelineRef) attached to a sentinel div at the bottom; useEffect on
          events.length scrolls timelineRef.current?.scrollIntoView({ behavior: 'smooth' }).

      Controls row (class "run-controls"):
        - actionError: shown in div class "action-error" when non-null
        - Advance/Continue button (class "btn btn-advance"): visible only when
          pendingApproval is null AND phase is not in
          ['COMPLETED', 'FAILED', 'ABORTED', 'WAITING_FOR_ACTIVITY_REVIEW'].
          Disabled when advancing. Calls handleAdvance().
        - When phase === 'WAITING_FOR_ACTIVITY_REVIEW': render "Review Activity Report"
          button (class "btn btn-activity") that calls onActivityReady().

    handleAbort():
      Sets aborting true, clears actionError.
      Calls abortRun(runId).
      On error: sets actionError to err.message.
      Always sets aborting false.

    handleAdvance():
      Sets advancing true, clears actionError.
      Calls advanceRun(runId).
      On success: calls refresh() to update pendingApproval/phase immediately.
      On error: sets actionError to err.message.
      Always sets advancing false.

    Extend frontend/src/index.css with styles for:
      run-view, run-header, customer-system-info, phase-label, connection-indicator,
      approval-slot, approval-card, approval-header, approval-command, approval-meta,
      approval-error, edit-command-textarea, reject-reason-textarea,
      btn-approve, btn-edit, btn-reject, btn-abort, btn-advance, btn-activity,
      timeline, timeline-event, event-time, event-icon, event-label, event-command,
      event-summary, run-controls, action-error, risk-badge,
      badge--safe, badge--low, badge--medium, badge--high.

    Risk badge background colors (double-dash class names):
      badge--safe: background #d4edda; color #155724
      badge--low: background #fff3cd; color #856404
      badge--medium: background #ffe0b2; color #7c4700
      badge--high: background #f8d7da; color #721c24
  </action>
  <verify>
    <automated>cd /Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    RunView.tsx compiles with no tsc errors.
    Prop interface matches the stub from plan 08-03 exactly (includes customerSystem: CustomerSystem | null).
    customerSystem fields (ip, port, username, os) rendered in the header.
    pendingApproval and phase are derived from useRun(runId).run, NOT from useRunEvents.
    ApprovalCard rendered when run.pendingApproval is non-null; onDecided calls refresh().
    refresh() also called after advanceRun() and on key SSE events.
    Timeline renders events with timestamp, icon, label; keepalive events never appear (no listener registered).
    Abort and Advance buttons present with correct disabled/visibility logic.
    WAITING_FOR_ACTIVITY_REVIEW triggers "Review Activity Report" button.
    index.css extended with all new class names using badge--safe/low/medium/high (not risk-safe etc.).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE payload → timeline | event.payload.command and event.payload.summary are backend strings rendered as text |
| Approval card controls → backend | editedCommand sent to /approve; backend re-runs safety gate |
| customerSystem → header display | ip/port/username/os from backend rendered as React text children |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | XSS | timeline event-command pre element | mitigate | Rendered as text content (pre element with React text children); never dangerouslySetInnerHTML |
| T-08-11 | XSS | approval command display | mitigate | Rendered in pre element as text children — React auto-escapes |
| T-08-12 | Tampering | edited command approval bypass | mitigate | Frontend sends editedCommand to backend /approve; backend re-runs deterministic safety gate (422 on block); frontend surfaces 422 error — no client-side safety bypass |
| T-08-13 | Information Disclosure | SSH keys / Phoenix token via SSE events | accept | Backend redacts all secrets before writing audit log and emitting SSE; frontend renders redacted strings only — no de-redaction logic in client |
| T-08-14 | Spoofing | SSE backfill injection | accept | SSE connects to same-origin backend via VITE_API_BASE via getEventsUrl() from api.ts; CORS handled server-side; no cross-origin SSE source |
| T-08-SC | Tampering | npm installs | accept | No new npm packages added this plan |
</threat_model>

<verification>
- tsc --noEmit clean after both tasks
- RunView imports riskBadge from mappers.ts (via ApprovalCard); no reference to non-existent riskBadge.ts
- ApprovalCard renders all three modes (default/edit/reject) based on internal state
- RunView timeline renders one row per non-keepalive SseEvent
- Risk badges use badge--safe/low/medium/high CSS classes (double-dash convention from plan 08-01)
- pendingApproval and phase come from useRun().run, not from useRunEvents
- refresh() called after approve/reject decision and on key SSE event types
- customerSystem header renders ip, port, username, os (fallback "–" when null)
- Abort button always visible; Advance button hidden on terminal phases
- WAITING_FOR_ACTIVITY_REVIEW triggers onActivityReady path
</verification>

<success_criteria>
- frontend/src/components/ApprovalCard.tsx implements the three-mode approval UI
- frontend/src/components/RunView.tsx replaces the stub with full run page including customer system header
- tsc --noEmit passes
- index.css includes risk badge colors using badge--safe/low/medium/high class names
- RunView does not read pendingApproval or phase from useRunEvents (those come from useRun)
</success_criteria>

<output>
Create `.planning/phases/08-frontend/08-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| RunView | frontend/src/components/RunView.tsx | component (replaces stub) |
| ApprovalCard | frontend/src/components/ApprovalCard.tsx | component |
