---
phase: 01-repo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - backend/package.json
  - backend/tsconfig.json
  - backend/Dockerfile
  - backend/.dockerignore
  - docker-compose.yml
  - backend/src/index.ts
  - backend/src/app.ts
  - backend/src/env.ts
  - backend/src/routes/health.ts
  - backend/src/routes/tickets.ts
  - backend/src/routes/runs.ts
  - backend/src/routes/approvals.ts
  - backend/src/routes/activity.ts
  - backend/src/routes/events.ts
  - backend/src/phoenix/client.ts
  - backend/src/phoenix/mock.ts
  - backend/src/phoenix/types.ts
  - backend/src/ssh/client.ts
  - backend/src/ssh/executor.ts
  - backend/src/ssh/mock.ts
  - backend/src/ssh/types.ts
  - backend/src/ai/model.ts
  - backend/src/ai/prompts.ts
  - backend/src/ai/orchestrator.ts
  - backend/src/ai/agents/problem-analyzer.ts
  - backend/src/ai/agents/customer-system-analyzer.ts
  - backend/src/ai/agents/problem-solver.ts
  - backend/src/ai/agents/validator.ts
  - backend/src/ai/agents/activity-log-generator.ts
  - backend/src/ai/tools/phoenix-tools.ts
  - backend/src/ai/tools/ssh-tools.ts
  - backend/src/ai/tools/audit-tools.ts
  - backend/src/ai/tools/safety-tools.ts
  - backend/src/safety/command-policy.ts
  - backend/src/safety/classifier.ts
  - backend/src/safety/redaction.ts
  - backend/src/safety/risk-levels.ts
  - backend/src/store/db.ts
  - backend/src/store/schema.ts
  - backend/src/store/runs.ts
  - backend/src/store/audit.ts
  - backend/src/events/run-event-bus.ts
  - backend/src/events/sse.ts
  - backend/src/tests/safety.test.ts
  - backend/src/tests/phoenix-client.test.ts
  - backend/src/tests/orchestrator.test.ts
  - .gitignore
  - .env.example
  - frontend/package.json
  # deletions (tracked for executor awareness):
  # backend/app/__init__.py  → deleted
  # backend/app/main.py      → deleted
  # backend/requirements.txt → deleted
  # frontend/package-lock.json → deleted
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03

must_haves:
  truths:
    - "pnpm-workspace.yaml exists at repo root listing backend and frontend packages"
    - "backend/Dockerfile uses node:22-slim, corepack enable, pnpm install --frozen-lockfile, CMD pnpm tsx src/index.ts"
    - "All backend/src/ subdirectories from ARCHITECTURE.md §2 exist with stub entry files"
    - ".env and keys/ remain git-ignored; .env.example is committed with placeholders only"
    - "Python skeleton (backend/app/, backend/requirements.txt) is deleted"
    - "frontend/package-lock.json is deleted; frontend/package.json unchanged"
    - "Backend container builds successfully from the new Dockerfile (docker compose build exits 0)"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "pnpm workspace root listing backend and frontend"
      contains: "packages:"
    - path: "backend/package.json"
      provides: "backend Node package with hono, @hono/node-server, zod, tsx, vitest"
    - path: "backend/tsconfig.json"
      provides: "TypeScript config targeting ES2022, NodeNext module resolution"
    - path: "backend/Dockerfile"
      provides: "node:22-slim image with corepack + pnpm + tsx dev runner"
      contains: "node:22-slim"
    - path: "backend/src/env.ts"
      provides: "stub placeholder — full impl in Plan 02"
      contains: "export"
    - path: "backend/src/app.ts"
      provides: "stub placeholder — full impl in Plan 03"
      contains: "export"
    - path: "backend/src/index.ts"
      provides: "stub placeholder — full impl in Plan 03"
      contains: "export"
    - path: "backend/src/routes/health.ts"
      provides: "stub placeholder — full impl in Plan 03"
      contains: "export"
    - path: "backend/src/safety/risk-levels.ts"
      provides: "RiskLevel enum stub"
      contains: "RiskLevel"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "backend/package.json"
      via: "pnpm workspace packages listing"
      pattern: "packages:"
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "build: ./backend"
      pattern: "node:22-slim"
---

<objective>
Replace the dead Python/FastAPI skeleton with the locked Node 22 + Hono + TypeScript scaffold.
Create the full backend/src/ directory tree from ARCHITECTURE.md §2 with stub entry files for
every module. Establish the pnpm workspace, rewrite the backend Dockerfile, update .env.example
with placeholder keys, and delete the Python artifacts. This plan produces the structural skeleton
all later plans build into — no real logic yet, just the wiring harness.

Purpose: Every downstream phase (2–9) imports from the module paths established here. Getting
the folder structure, package names, and Dockerfile right now prevents path-chasing later.

Output: pnpm workspace, rewritten Dockerfile, full backend/src/ tree (stubs), .env.example updated,
Python skeleton deleted, frontend/package-lock.json deleted.
</objective>

<execution_context>
@/Users/julianschmidt/.claude/gsd-core/workflows/execute-plan.md
@/Users/julianschmidt/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/ROADMAP.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.planning/phases/01-repo-foundation/01-CONTEXT.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/docs/ARCHITECTURE.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/docs/TASKS.md
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/docker-compose.yml
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.gitignore
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/.env.example
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/package.json
@/Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish pnpm workspace and backend Node package</name>
  <read_first>
    docs/ARCHITECTURE.md §1 (stack decision: Node 22 + pnpm + tsx), §2 (folder structure),
    §10 (config rules). frontend/package.json (preserve scripts/deps, only lockfile changes).
    .env.example (current contents — add new keys without removing existing ones).
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-03: pnpm workspace, D-04: Dockerfile shape).
  </read_first>
  <files>
    pnpm-workspace.yaml,
    backend/package.json,
    backend/tsconfig.json,
    .env.example
  </files>
  <action>
    Create pnpm-workspace.yaml at repo root. It must list exactly two packages:
    "backend" and "frontend" under the packages key. This is per D-03.

    Create backend/package.json with:
    - name "techbold-track-backend", version "0.1.0", private true, type "module"
    - scripts: "dev" runs "tsx src/index.ts", "start" runs "tsx src/index.ts",
      "test" runs "vitest run"
    - dependencies: hono (latest stable 4.x), @hono/node-server (latest stable),
      @hono/zod-validator, zod, tsx, better-sqlite3, ssh2, ai (Vercel AI SDK v5 — package
      name is "ai"), @ai-sdk/openai
    - devDependencies: typescript, @types/node, vitest, @types/better-sqlite3, @types/ssh2
    Do NOT include a lockfile — pnpm will generate pnpm-lock.yaml at the root.

    Create backend/tsconfig.json with:
    - target ES2022, module NodeNext, moduleResolution NodeNext
    - strict true, esModuleInterop true, skipLibCheck true
    - outDir dist (unused in dev but needed for tsc type-check)
    - rootDir src, include ["src/**/*"]

    Rewrite .env.example to include ALL placeholder keys this project needs, with no real values.
    Keep any existing keys already present. Add (or confirm presence of):
    - PHOENIX_API_URL=https://erp.example.com
    - PHOENIX_API_TOKEN=your-phoenix-token-here
    - LLM_PROVIDER=openai
    - LLM_MODEL=gpt-4o
    - OPENAI_API_KEY=your-openai-key-here
    - SSH_KEY_PATH=/keys/id_rsa
    - MOCK_MODE=false
    - MOCK_PHOENIX=false
    - MOCK_SSH=false
    - MOCK_LLM=false
    Every value is a placeholder string — no real credentials. Per PLAT-03 and TASKS P0-2.
  </action>
  <verify>
    <automated>
      cd /Users/julianschmidt/Documents/GitHub/techbold_track_template && cat pnpm-workspace.yaml | grep -c "backend" && cat pnpm-workspace.yaml | grep -c "frontend" && node -e "const p=JSON.parse(require('fs').readFileSync('backend/package.json','utf8')); console.log(p.name, p.type, Object.keys(p.dependencies).join(','))"
    </automated>
  </verify>
  <done>
    pnpm-workspace.yaml lists both packages. backend/package.json has type "module", correct
    deps (hono, zod, tsx, vitest, ai). backend/tsconfig.json has NodeNext moduleResolution.
    .env.example has all 10 keys as placeholders, no real values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite backend Dockerfile, create .dockerignore, wire compose build context, delete Python skeleton</name>
  <read_first>
    backend/Dockerfile (current python:3.11-slim content — will be fully replaced).
    docker-compose.yml (current build stanza — must be updated to context: . so the full
    workspace is available to the Dockerfile).
    .planning/phases/01-repo-foundation/01-CONTEXT.md D-04 (exact Dockerfile shape).
    .gitignore (confirm backend/app/ and requirements.txt are not explicitly tracked).
  </read_first>
  <files>
    backend/Dockerfile,
    backend/.dockerignore,
    docker-compose.yml
  </files>
  <action>
    Rewrite backend/Dockerfile completely per D-04:
    - FROM node:22-slim
    - WORKDIR /app
    - RUN corepack enable
    - COPY pnpm-workspace.yaml ./
    - COPY backend/package.json backend/
    - COPY frontend/package.json frontend/
    - RUN pnpm install --frozen-lockfile
    - COPY backend/src ./backend/src
    - EXPOSE 8000
    - CMD ["pnpm", "--filter", "backend", "tsx", "src/index.ts"]

    Update docker-compose.yml backend service build stanza to use the repo root as context:
      build:
        context: .
        dockerfile: backend/Dockerfile
    This is required because the Dockerfile COPYs pnpm-workspace.yaml and frontend/package.json
    from the repo root — a ./backend context cannot reach them. Preserve all other docker-compose.yml
    fields unchanged: ./keys:/keys:ro volume mount, extra_hosts, env_file (required: false), port
    mapping 8000:8000, VITE_API_BASE for frontend service.

    Create backend/.dockerignore with:
    - node_modules
    - dist
    - *.tsbuildinfo
    - src/tests
    - .env

    Delete the Python skeleton:
    - Delete backend/app/__init__.py
    - Delete backend/app/main.py
    - Delete backend/app/ directory
    - Delete backend/requirements.txt
    - Delete frontend/package-lock.json (replaced by pnpm workspace per D-03)

    After deleting, confirm keys/.gitkeep still exists AND run:
      git check-ignore keys/id_rsa.pem
    to confirm keys/ is git-ignored (PLAT-03). If keys/.gitkeep does not exist, create it.
  </action>
  <verify>
    <automated>
      grep "node:22-slim" /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/Dockerfile && grep "corepack enable" /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/Dockerfile && test ! -f /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/requirements.txt && test ! -d /Users/julianschmidt/Documents/GitHub/techbold_track_template/backend/app && test ! -f /Users/julianschmidt/Documents/GitHub/techbold_track_template/frontend/package-lock.json && grep "context: \." /Users/julianschmidt/Documents/GitHub/techbold_track_template/docker-compose.yml && git -C /Users/julianschmidt/Documents/GitHub/techbold_track_template check-ignore keys/id_rsa.pem && echo "all checks passed"
    </automated>
  </verify>
  <done>
    backend/Dockerfile uses node:22-slim + corepack enable + pnpm tsx CMD.
    backend/.dockerignore exists.
    docker-compose.yml backend build uses context: . and dockerfile: backend/Dockerfile.
    backend/app/ directory and backend/requirements.txt are deleted.
    frontend/package-lock.json is deleted.
    keys/.gitkeep exists AND git check-ignore keys/id_rsa.pem confirms keys/ is git-ignored (PLAT-03).
    docker-compose.yml preserves ./keys:/keys:ro mount, extra_hosts, env_file required:false.
  </done>
</task>

<task type="auto">
  <name>Task 3a: Scaffold backend/src/ routes, phoenix, ssh, store, events, safety stubs and test stubs</name>
  <read_first>
    docs/ARCHITECTURE.md §2 (exact folder structure) and §3 (component responsibilities —
    use these to write the one-line module-purpose comment on each stub).
    .planning/phases/01-repo-foundation/01-CONTEXT.md D-01/D-02 (mock seam contract).
    .gitignore (confirm node_modules and dist already present; add backend/dist/ if missing).
  </read_first>
  <files>
    backend/src/index.ts,
    backend/src/app.ts,
    backend/src/env.ts,
    backend/src/routes/health.ts,
    backend/src/routes/tickets.ts,
    backend/src/routes/runs.ts,
    backend/src/routes/approvals.ts,
    backend/src/routes/activity.ts,
    backend/src/routes/events.ts,
    backend/src/phoenix/client.ts,
    backend/src/phoenix/mock.ts,
    backend/src/phoenix/types.ts,
    backend/src/ssh/client.ts,
    backend/src/ssh/executor.ts,
    backend/src/ssh/mock.ts,
    backend/src/ssh/types.ts,
    backend/src/safety/command-policy.ts,
    backend/src/safety/classifier.ts,
    backend/src/safety/redaction.ts,
    backend/src/safety/risk-levels.ts,
    backend/src/store/db.ts,
    backend/src/store/schema.ts,
    backend/src/store/runs.ts,
    backend/src/store/audit.ts,
    backend/src/events/run-event-bus.ts,
    backend/src/events/sse.ts,
    backend/src/tests/safety.test.ts,
    backend/src/tests/phoenix-client.test.ts,
    backend/src/tests/orchestrator.test.ts,
    .gitignore
  </files>
  <action>
    Create backend/src/ entry-point stubs and all non-AI subsystem stubs. Files that will be
    fully implemented in later plans get a typed stub export so TypeScript resolves them now.

    Entry-point stubs (Plans 02/03 replace these):
    - backend/src/env.ts: export const env = {} as Record&lt;string, string&gt;;
      // stub — implemented in Plan 02
    - backend/src/app.ts: import { Hono } from 'hono'; export const app = new Hono();
      // stub — implemented in Plan 03
    - backend/src/index.ts: import { serve } from '@hono/node-server'; import { app } from './app';
      serve({ fetch: app.fetch, port: 8000 });
      // stub — implemented in Plan 03
    - backend/src/routes/health.ts: import { Hono } from 'hono';
      export const healthRouter = new Hono();
      healthRouter.get('/', (c) => c.json({ status: 'ok' }));
      // stub — implemented in Plan 03

    Route stubs (tickets, runs, approvals, activity, events): one-line comment naming the module
    + "export {}" to satisfy TypeScript resolution. Example:
      // Ticket proxy routes — implemented in Phase 2
      export {}

    Phoenix stubs (client, mock, types): same pattern.
      // Phoenix ERP client — implemented in Phase 2
      export {}

    SSH stubs (client, executor, mock, types): same pattern.
      // SSH executor — implemented in Phase 4
      export {}

    Safety stubs:
    - backend/src/safety/command-policy.ts: "// Command blocklist policy — implemented in Phase 3\nexport {}"
    - backend/src/safety/classifier.ts: "// Risk classifier — implemented in Phase 3\nexport {}"
    - backend/src/safety/redaction.ts: "// Secret redaction — implemented in Phase 3\nexport {}"
    - backend/src/safety/risk-levels.ts: REAL implementation — export enum RiskLevel {
        SAFE_READ_ONLY = 'SAFE_READ_ONLY',
        LOW_RISK_CHANGE = 'LOW_RISK_CHANGE',
        MEDIUM_RISK_CHANGE = 'MEDIUM_RISK_CHANGE',
        HIGH_RISK_BLOCKED = 'HIGH_RISK_BLOCKED'
      }
      This is a real type used by Phase 3 — define it now so the folder seam is useful.

    Store stubs (db, schema, runs, audit): comment + "export {}"
    Events stubs (run-event-bus, sse): comment + "export {}"

    Test stubs (backend/src/tests/*.test.ts): one-line stubs with a comment identifying
    the test target and a single skipped describe block so Vitest can discover them:
      import { describe } from 'vitest';
      describe.skip('placeholder', () => {});

    Update .gitignore if it does not already exclude backend/dist/ — add it alongside
    the existing frontend/dist/ line. Do not change any existing ignore patterns.

    Do NOT create any ai/ subtree files in this task — those are Task 3b.
    Do NOT implement any business logic. Stubs only.
  </action>
  <verify>
    <automated>
      cd /Users/julianschmidt/Documents/GitHub/techbold_track_template && test -f backend/src/safety/risk-levels.ts && grep -c "RiskLevel" backend/src/safety/risk-levels.ts && test -f backend/src/tests/safety.test.ts && grep -c "describe.skip" backend/src/tests/safety.test.ts && test -f backend/src/routes/tickets.ts && test -f backend/src/store/audit.ts && test -f backend/src/events/sse.ts && echo "Task 3a scaffold verified"
    </automated>
  </verify>
  <done>
    All routes/, phoenix/, ssh/, safety/, store/, events/ subdirectories exist with stub files.
    backend/src/safety/risk-levels.ts exports RiskLevel enum with all four values.
    All three test stubs are Vitest-discoverable (describe.skip).
    Entry-point stubs (env.ts, app.ts, index.ts, routes/health.ts) exist with typed exports.
    backend/dist/ is in .gitignore.
  </done>
</task>

<task type="auto">
  <name>Task 3b: Scaffold backend/src/ai/ subtree stubs (agents, tools, orchestrator, model, prompts)</name>
  <read_first>
    docs/ARCHITECTURE.md §2 (ai/ subfolder layout) and §7 (agent filenames must use the
    brief's vocabulary: problem-analyzer, customer-system-analyzer, problem-solver, validator,
    activity-log-generator). §3 (agent and orchestrator responsibilities — use for stub comments).
    .planning/phases/01-repo-foundation/01-CONTEXT.md (D-01: AI never executes SSH; agents are
    prompts + Zod schemas invoked by orchestrator).
  </read_first>
  <files>
    backend/src/ai/model.ts,
    backend/src/ai/prompts.ts,
    backend/src/ai/orchestrator.ts,
    backend/src/ai/agents/problem-analyzer.ts,
    backend/src/ai/agents/customer-system-analyzer.ts,
    backend/src/ai/agents/problem-solver.ts,
    backend/src/ai/agents/validator.ts,
    backend/src/ai/agents/activity-log-generator.ts,
    backend/src/ai/tools/phoenix-tools.ts,
    backend/src/ai/tools/ssh-tools.ts,
    backend/src/ai/tools/audit-tools.ts,
    backend/src/ai/tools/safety-tools.ts
  </files>
  <action>
    Create all ai/ subtree stubs. Every file gets a one-line comment naming the module and
    its role per ARCHITECTURE.md §3, plus "export {}" for TypeScript resolution.

    - backend/src/ai/model.ts: "// AI model selector — implemented in Phase 2\nexport {}"
    - backend/src/ai/prompts.ts: "// Agent prompt templates — implemented in Phase 2\nexport {}"
    - backend/src/ai/orchestrator.ts: "// Run state machine orchestrator — implemented in Phase 5\nexport {}"

    Agents (all stubs, implemented in Phase 5+):
    - backend/src/ai/agents/problem-analyzer.ts:
      "// Problem analyzer agent — proposes one diagnostic command + ranked hypotheses\nexport {}"
    - backend/src/ai/agents/customer-system-analyzer.ts:
      "// Customer system analyzer agent — profiles the target VM\nexport {}"
    - backend/src/ai/agents/problem-solver.ts:
      "// Problem solver agent — proposes minimal reversible fix\nexport {}"
    - backend/src/ai/agents/validator.ts:
      "// Validator agent — confirms fix succeeded\nexport {}"
    - backend/src/ai/agents/activity-log-generator.ts:
      "// Activity log generator agent — drafts ERP report from audit trail\nexport {}"

    Tools (all stubs — NOTE: executeApprovedCommand is NEVER registered as a model tool per
    ARCHITECTURE.md anti-patterns §A1):
    - backend/src/ai/tools/phoenix-tools.ts:
      "// Phoenix read tools for model context — implemented in Phase 2\nexport {}"
    - backend/src/ai/tools/ssh-tools.ts:
      "// SSH tools — proposeSshCommand only; execution is backend-only (never a model tool)\nexport {}"
    - backend/src/ai/tools/audit-tools.ts:
      "// Audit read tools for model context — implemented in Phase 5\nexport {}"
    - backend/src/ai/tools/safety-tools.ts:
      "// Safety classification tools — implemented in Phase 3\nexport {}"

    Do NOT implement any logic. Stubs only. The anti-pattern note in ssh-tools.ts is intentional —
    leave it as a guardrail comment for the Phase 4/5 executor.
  </action>
  <verify>
    <automated>
      cd /Users/julianschmidt/Documents/GitHub/techbold_track_template && test -f backend/src/ai/agents/problem-analyzer.ts && test -f backend/src/ai/agents/activity-log-generator.ts && test -f backend/src/ai/tools/ssh-tools.ts && test -f backend/src/ai/orchestrator.ts && find backend/src/ai -name "*.ts" | wc -l && echo "Task 3b ai/ scaffold verified"
    </automated>
  </verify>
  <done>
    All 12 ai/ files exist: model.ts, prompts.ts, orchestrator.ts, 5 agents, 4 tools.
    Agent filenames match ARCHITECTURE.md §7 vocabulary exactly.
    ssh-tools.ts stub contains the executeApprovedCommand anti-pattern guardrail comment.
    find backend/src/ai -name "*.ts" returns 12.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → git | .env and keys/ must never cross this boundary |
| container → host | SSH keys flow one-way: host ./keys → /keys:ro mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example | mitigate | Contains only placeholder strings; enforced by reviewer on every commit. No real values. |
| T-01-02 | Information Disclosure | .gitignore | mitigate | .env and keys/* already ignored. Add backend/dist/ to ensure build artifacts don't leak. Verify with `git check-ignore -v .env` after scaffold. |
| T-01-03 | Tampering | frontend/package-lock.json deletion | accept | Lockfile is replaced by pnpm-lock.yaml at root — same reproducibility guarantee, different manager. No security regression. |
| T-01-04 | Information Disclosure | backend/.dockerignore | mitigate | Excludes .env and node_modules from image context. SSH key path is a runtime env var, not baked into the image. |
| T-01-SC | Tampering | npm/pip package installs | mitigate | No new package installs in this plan — only package.json authored by this plan. pnpm install runs in Plan 03 verification. Packages are well-known (hono, zod, tsx, vitest, ai, ssh2, better-sqlite3). |
</threat_model>

<verification>
After all four tasks complete:

1. Confirm Python artifacts are gone: `test ! -d backend/app && test ! -f backend/requirements.txt`
2. Confirm pnpm workspace: `cat pnpm-workspace.yaml`
3. Confirm Dockerfile base: `head -1 backend/Dockerfile` → "FROM node:22-slim"
4. Confirm compose build context: `grep "context: \." docker-compose.yml`
5. Confirm .env.example has no real credentials: `grep -v "^#" .env.example | grep -v "^$" | grep -v "your-" | grep -v "false" | grep -v "openai" | grep -v "gpt"` — output should be empty or only placeholder-safe values
6. Confirm git-ignore covers .env and keys/: `git check-ignore -v .env` and `git check-ignore keys/id_rsa.pem`
7. Confirm scaffold count: `find backend/src -name "*.ts" | wc -l` → 38 or more
8. Confirm RiskLevel enum: `grep "HIGH_RISK_BLOCKED" backend/src/safety/risk-levels.ts`
9. Confirm ai/ subtree: `find backend/src/ai -name "*.ts" | wc -l` → 12
</verification>

<success_criteria>
- pnpm-workspace.yaml exists at repo root listing backend and frontend
- backend/Dockerfile uses node:22-slim, corepack enable, pnpm tsx CMD (per D-04)
- docker-compose.yml backend build uses context: . and dockerfile: backend/Dockerfile
- All 38+ backend/src/ files from ARCHITECTURE.md §2 exist (stubs are fine for non-Plan-02/03 files)
- RiskLevel enum is defined and exported from backend/src/safety/risk-levels.ts
- backend/app/ directory and backend/requirements.txt do not exist
- frontend/package-lock.json does not exist
- .env.example has all 10 required env keys as placeholders; no real secrets
- .env and keys/ remain git-ignored (git check-ignore confirms both)
- keys/.gitkeep exists; git check-ignore keys/id_rsa.pem confirms keys/ is git-ignored (PLAT-03)
- docker-compose.yml preserves ./keys:/keys:ro mount, extra_hosts, env_file required:false
</success_criteria>

<output>
Create .planning/phases/01-repo-foundation/01-01-SUMMARY.md when done.

Artifacts this phase produces (Plan 01 contribution):
- NEW FILE: pnpm-workspace.yaml — workspace root
- NEW FILE: backend/package.json — Node package (hono, zod, tsx, vitest, ai, ssh2, better-sqlite3)
- NEW FILE: backend/tsconfig.json — NodeNext, strict, ES2022
- NEW FILE: backend/Dockerfile — node:22-slim + corepack + pnpm tsx runner
- NEW FILE: backend/.dockerignore
- MODIFIED: docker-compose.yml — backend build context changed to repo root
- NEW FILE: backend/src/env.ts — stub (Plan 02 replaces with Zod parser)
- NEW FILE: backend/src/app.ts — stub Hono app export
- NEW FILE: backend/src/index.ts — stub serve() bootstrap
- NEW FILE: backend/src/routes/health.ts — stub GET /health
- NEW FILE: backend/src/routes/tickets.ts — stub
- NEW FILE: backend/src/routes/runs.ts — stub
- NEW FILE: backend/src/routes/approvals.ts — stub
- NEW FILE: backend/src/routes/activity.ts — stub
- NEW FILE: backend/src/routes/events.ts — stub
- NEW FILE: backend/src/phoenix/client.ts — stub
- NEW FILE: backend/src/phoenix/mock.ts — stub
- NEW FILE: backend/src/phoenix/types.ts — stub
- NEW FILE: backend/src/ssh/client.ts — stub
- NEW FILE: backend/src/ssh/executor.ts — stub
- NEW FILE: backend/src/ssh/mock.ts — stub
- NEW FILE: backend/src/ssh/types.ts — stub
- NEW FILE: backend/src/ai/model.ts — stub
- NEW FILE: backend/src/ai/prompts.ts — stub
- NEW FILE: backend/src/ai/orchestrator.ts — stub
- NEW FILE: backend/src/ai/agents/problem-analyzer.ts — stub
- NEW FILE: backend/src/ai/agents/customer-system-analyzer.ts — stub
- NEW FILE: backend/src/ai/agents/problem-solver.ts — stub
- NEW FILE: backend/src/ai/agents/validator.ts — stub
- NEW FILE: backend/src/ai/agents/activity-log-generator.ts — stub
- NEW FILE: backend/src/ai/tools/phoenix-tools.ts — stub
- NEW FILE: backend/src/ai/tools/ssh-tools.ts — stub
- NEW FILE: backend/src/ai/tools/audit-tools.ts — stub
- NEW FILE: backend/src/ai/tools/safety-tools.ts — stub
- NEW FILE: backend/src/safety/command-policy.ts — stub
- NEW FILE: backend/src/safety/classifier.ts — stub
- NEW FILE: backend/src/safety/redaction.ts — stub
- NEW FILE: backend/src/safety/risk-levels.ts — REAL: exports RiskLevel enum (SAFE_READ_ONLY | LOW_RISK_CHANGE | MEDIUM_RISK_CHANGE | HIGH_RISK_BLOCKED)
- NEW FILE: backend/src/store/db.ts — stub
- NEW FILE: backend/src/store/schema.ts — stub
- NEW FILE: backend/src/store/runs.ts — stub
- NEW FILE: backend/src/store/audit.ts — stub
- NEW FILE: backend/src/events/run-event-bus.ts — stub
- NEW FILE: backend/src/events/sse.ts — stub
- NEW FILE: backend/src/tests/safety.test.ts — discoverable stub (describe.skip)
- NEW FILE: backend/src/tests/phoenix-client.test.ts — discoverable stub
- NEW FILE: backend/src/tests/orchestrator.test.ts — discoverable stub
- MODIFIED: .env.example — adds MOCK_MODE, MOCK_PHOENIX, MOCK_SSH, MOCK_LLM, PHOENIX_API_URL, PHOENIX_API_TOKEN, LLM_PROVIDER, LLM_MODEL, OPENAI_API_KEY, SSH_KEY_PATH
- MODIFIED: .gitignore — adds backend/dist/ if not present
- DELETED: backend/app/__init__.py, backend/app/main.py, backend/app/
- DELETED: backend/requirements.txt
- DELETED: frontend/package-lock.json
</output>
