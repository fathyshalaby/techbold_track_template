# Secrets & keys — NEVER commit these
.env
.env.*
!.env.example
keys/*
!keys/.gitkeep
*.pem
*.key
# Hackathon-provided credentials & material (Azure key, Phoenix token, SSH keys, VM info)
important_stuff.txt
*secret*
*credentials*
tb-hackathon-ssh/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Node
node_modules/
frontend/dist/
*.tsbuildinfo

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Self-evolving solution memory (local SQLite — never commit)
backend-py/data/
solutions.db
